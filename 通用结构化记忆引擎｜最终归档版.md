# 通用结构化记忆引擎｜最终归档版（PostgreSQL\+tsvector 唯一存储）

## 0\. 整体定稿说明（归档核心约束）

本文为 **Subhuti Agent 结构化记忆引擎最终定稿方案**，永久归档，不再改动架构。

硬性约束（全局唯一）：

- **唯一持久存储：PostgreSQL**

- **唯一全文检索：PG tsvector / tsquery**

- 无 SQLite、无 FTS5、无向量依赖、无第三方重型图数据库

- 架构：物理双层 \+ 逻辑冷热 \+ 结构三层 \+ 领域插件化

- 包含 P0/P1/P2/P3 全部优化，工业级闭环

## 1\. 整体架构总览（最终定稿）

完整四层六边形架构拓扑图（唯一PG存储版）

```plain
┌───────────────────────────────────────────────────────────────────┐
│  【入站端口】 MemoryEnginePort                                    │
│  集合管理 / 节点CRUD / 检索查询 / 沉淀同步 / 热度管理 / 反馈校验  │
├───────────────────────────────────────────────────────────────────┤
│  【核心引擎层】 Memory Core Engine                                │
│  ┌────────────┬────────────┬────────────┬────────────┐          │
│  │ 树结构管理  │ 检索调度器  │ 热度生命周期│ 领域路由中心│          │
│  │ ✅树结构校验│ ✅三级递进  │ ✅冷热流转  │ ✅适配器注册│          │
│  │ ✅幂等写入  │ ✅重排序    │ ✅自动衰减  │ ✅分词路由  │          │
│  │ ✅去重合流  │ ✅上下文组装│ ✅记忆压缩  │ ✅增量解析  │          │
│  ├────────────┼────────────┼────────────┼────────────┤          │
│  │ 关联图谱管理│ 索引构建器  │ 版本快照管理│ 事件分发器  │          │
│  │ ✅双向引用  │ ✅BM25构建 │ ✅版本打标  │ ✅变更事件  │          │
│  │ ✅跨域关联  │ ✅增量更新  │ ✅快照切换  │ ✅可观测    │          │
│  └────────────┴────────────┴────────────┴────────────┘          │
│  ★ 内核仅操作通用记忆节点模型，零领域业务逻辑、零外部强依赖       │
├───────────────────────────────────────────────────────────────────┤
│  【领域适配层】 Domain Adapter（可插拔 Trait）                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ Rust 编程域  │  │ Blender建模域│  │ 通用文档域  │             │
│  │Tree-Sitter  │  │ 场景解析器   │  │ Markdown    │             │
│  │✅增量解析    │  │ ✅资产映射    │  │ 章节拆分    │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│  统一契约：Parser + Tokenizer + Ranker + Formatter              │
├───────────────────────────────────────────────────────────────────┤
│  【出站存储端口】 Storage & Index                                │
│  ┌──────────────────────┐  ┌──────────────────────┐            │
│  │ 内存存储（临时/热缓存）│  │ 数据库存储（长期持久）│            │
│  │ HashMap + 内存BM25    │  │ PostgreSQL + tsvector │            │
│  │ ✅热节点常驻          │  │ ✅全量节点 + 关联边   │            │
│  │ ✅会话临时记忆        │  │ ✅全局全文索引持久化 │            │
│  └──────────────────────┘  └──────────────────────┘            │
└───────────────────────────────────────────────────────────────────┘
```

四层完全正交、解耦、六边形架构：

1. **物理存储层**：内存（高速缓存）\+ PostgreSQL（唯一持久）

2. **逻辑生命周期层**：热记忆 / 冷记忆 / 临时会话记忆（动态流转）

3. **记忆结构层**：树结构（纵向层级）\+ BM25/ts检索（词法）\+ 轻量图谱（横向关联）

4. **领域适配层**：可插拔解析、切片、摘要、排序、格式化

核心流水线：

- **写入**：原始内容 → 领域语义切片 → 标准化节点 → 幂等去重 → 树校验 → 图谱关联 → 内存写入 → 异步PG落库 \+ 自动ts索引

- **查询**：领域分词扩展 → 三级检索（临时→热内存→PG冷库）→ 结构化上下文补全 → 领域重排序 → 动态Token组装 → 热度更新

四层完全正交、解耦、六边形架构：

1. **物理存储层**：内存（高速缓存）\+ PostgreSQL（唯一持久）

2. **逻辑生命周期层**：热记忆 / 冷记忆 / 临时会话记忆（动态流转）

3. **记忆结构层**：树结构（纵向层级）\+ BM25/ts检索（词法）\+ 轻量图谱（横向关联）

4. **领域适配层**：可插拔解析、切片、摘要、排序、格式化

核心流水线：

- **写入**：原始内容 → 领域语义切片 → 标准化节点 → 幂等去重 → 树校验 → 图谱关联 → 内存写入 → 异步PG落库 \+ 自动ts索引

- **查询**：领域分词扩展 → 三级检索（临时→热内存→PG冷库）→ 结构化上下文补全 → 领域重排序 → 动态Token组装 → 热度更新

## 2\. 分层精确定义（最终版）

### 2\.1 物理分层（真实存储介质）

**① 内存记忆（高速层）**

- 当前会话临时记忆（断电丢失）

- 长期热记忆常驻缓存（高频节点）

- 内存BM25索引（毫秒级检索）

**② PostgreSQL 持久记忆（唯一落库层）**

- 全量冷/热记忆、版本、快照、关联边

- PG tsvector 全局全文索引

- 唯一数据源，所有数据最终一致性以PG为准

### 2\.2 逻辑分层（激活热度，与物理层正交）

- **临时记忆**：仅内存、会话绑定、不持久

- **热记忆**：高激活分，内存缓存 \+ PG双存在

- **冷记忆**：低激活分，仅PG存储，访问后自动升温

### 2\.3 记忆结构三层（三位一体，不可替代）

- **记忆树（纵向）**：父子、层级、路径、结构上下文（解决“在哪里、属于谁”）

- **词法检索（BM25\+tsvector）**：关键词召回、模糊匹配（解决“快速找到”）

- **轻量知识图谱（横向边）**：调用、依赖、引用、跨域关联（解决“和谁有关、影响谁”）

### 2\.4 领域适配层（完全插件化）

内核零业务逻辑，所有领域差异由适配器实现：

- 语义切片规则（领域不统一，代码按AST、文档按标题、建模按资产）

- 摘要生成、元数据提取

- 查询分词、同义词扩展、停用词

- 检索重排序权重、惩罚规则

- 上下文格式化、临时记忆压缩

- 增量解析、diff更新

## 3\. 核心数据模型（最终定稿）

全局唯一通用节点，全领域、全分层复用

```rust

pub struct MemoryNode {
    // 唯一标识
    pub node_id: String,
    pub collection_id: String,
    pub domain: String,
    pub node_type: String,
    pub content_hash: String,

    // 树结构纵向
    pub parent_id: Option<String>,
    pub path: String,
    pub depth: u32,
    pub sort_order: u32,

    // 内容
    pub title: String,
    pub summary: String,
    pub content: String,
    pub metadata: Value,

    // 图谱横向关联
    pub refs_out: Vec<RefEdge>,
    pub refs_in: Vec<RefEdge>,

    // 版本快照
    pub version_tag: String,
    pub snapshot_id: Option<String>,

    // 热度生命周期
    pub base_activation: f32,
    pub importance: u8,
    pub access_count: u32,
    pub feedback_score: f32,
    pub last_accessed_at: i64,
    pub created_at: i64,
    pub updated_at: i64,
}

// 轻量跨域关联边
pub struct RefEdge {
    pub target_node_id: String,
    pub target_collection_id: String,
    pub edge_type: RefType,
    pub weight: f32,
}

pub enum RefType {
    Calls,
    Implements,
    DependsOn,
    Uses,
    Related
}
    
```

## 4\. PostgreSQL 最终 DDL（唯一存储、归档版）

自动维护 tsvector 全文索引，权重：标题A \> 摘要B \> 路径C

```sql

-- 集合
CREATE TABLE memory_collections (
    collection_id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    domain TEXT NOT NULL,
    description TEXT,
    created_at BIGINT NOT NULL
);

-- 主记忆节点
CREATE TABLE memory_nodes (
    node_id TEXT PRIMARY KEY,
    collection_id TEXT NOT NULL,
    domain TEXT NOT NULL,
    node_type TEXT NOT NULL,
    parent_id TEXT,
    path TEXT NOT NULL,
    depth INT NOT NULL,
    sort_order INT NOT NULL DEFAULT 0,
    title TEXT NOT NULL,
    summary TEXT NOT NULL,
    content TEXT NOT NULL,
    content_hash TEXT NOT NULL,
    metadata JSONB,
    version_tag TEXT NOT NULL DEFAULT 'current',
    snapshot_id TEXT,
    base_activation REAL NOT NULL DEFAULT 0.5,
    importance SMALLINT NOT NULL DEFAULT 1,
    access_count INT NOT NULL DEFAULT 0,
    feedback_score REAL NOT NULL DEFAULT 0.0,
    last_accessed_at BIGINT NOT NULL,
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL,

    FOREIGN KEY (collection_id) REFERENCES memory_collections(collection_id),
    UNIQUE(collection_id, version_tag, path, node_type, title)
);

-- 关联图谱边（支持跨领域、跨集合）
CREATE TABLE memory_edges (
    edge_id TEXT PRIMARY KEY,
    from_node_id TEXT NOT NULL,
    from_collection_id TEXT NOT NULL,
    to_node_id TEXT NOT NULL,
    to_collection_id TEXT NOT NULL,
    edge_type TEXT NOT NULL,
    weight REAL NOT NULL DEFAULT 1.0,
    created_at BIGINT NOT NULL,
    UNIQUE(from_node_id, to_node_id, edge_type)
);

-- 版本快照
CREATE TABLE memory_snapshots (
    snapshot_id TEXT PRIMARY KEY,
    collection_id TEXT NOT NULL,
    name TEXT NOT NULL,
    version_tag TEXT NOT NULL,
    description TEXT,
    created_at BIGINT NOT NULL,
    FOREIGN KEY (collection_id) REFERENCES memory_collections(collection_id)
);

-- PG 全文检索自动索引
ALTER TABLE memory_nodes ADD COLUMN fts_doc tsvector;

CREATE OR REPLACE FUNCTION memory_fts_update() RETURNS trigger AS $$
BEGIN
    NEW.fts_doc :=
        setweight(to_tsvector('simple', COALESCE(NEW.title,'')), 'A') ||
        setweight(to_tsvector('simple', COALESCE(NEW.summary,'')), 'B') ||
        setweight(to_tsvector('simple', COALESCE(NEW.path,'')), 'C');
    RETURN NEW;
END
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_memory_fts
BEFORE INSERT OR UPDATE ON memory_nodes
FOR EACH ROW EXECUTE FUNCTION memory_fts_update();

-- 业务索引
CREATE INDEX idx_nodes_collection ON memory_nodes(collection_id);
CREATE INDEX idx_nodes_parent ON memory_nodes(parent_id);
CREATE INDEX idx_nodes_path ON memory_nodes(path);
CREATE INDEX idx_nodes_hash ON memory_nodes(content_hash);
CREATE INDEX idx_nodes_activation ON memory_nodes(base_activation);
CREATE INDEX idx_edges_from ON memory_edges(from_node_id);
CREATE INDEX idx_edges_to ON memory_edges(to_node_id);
CREATE INDEX idx_nodes_fts ON memory_nodes USING GIN(fts_doc);
    
```

## 5\. P0\-P3 全量能力整合（最终内置）

### P0 基础稳健（强制内核）

- 幂等写入、业务唯一键防重复

- content\_hash 内容去重

- 树结构合法性校验（防环、防跨挂载、单根约束）

- 基础CRUD、结构完整性保障

### P1 性能与工程增强（生产必备）

- Tree\-Sitter 增量解析、局部更新

- 增量索引更新（不重建全量库）

- 动态三级上下文模式：精准/标准/深度

- 版本快照 \+ Git联动

- 事件总线全事件广播，可观测、可联动

### P2 自进化体验优化

- 临时记忆自动压缩，防上下文爆炸

- 用户/Agent 正负反馈闭环，自动调权

- 检索可解释性（得分明细、命中词、权重来源）

- 热度自衰减、自动遗忘机制

### P3 高级扩展（可选开启）

- 跨领域、跨集合图谱关联

- 混合检索预留扩展口（可选向量，默认关闭）

## 6\. 写入流水线（最终定稿）

输入校验 → 领域路由 → P0幂等去重前置 → 领域语义切片 → 摘要元数据增强 → 标准化节点 → 树结构挂载\+校验 → 图谱关联提取 → 内存写入 → 异步PG落库\+增量TS索引 → 热度初始化 → 事件推送

## 7\. 查询流水线（最终定稿）

查询预处理 → 领域分词\+扩展 → 三级检索（临时内存→热内存→PG冷库） → 去重\+基础打分 → 树结构上下文补全（父/子/关联） → 领域重排序 → 动态Token自适应组装 → 可解释信息附加 → 热度刷新\+冷热流转

## 8\. 冷热记忆算法（最终公式）

**综合激活分 = base\_activation × 时间衰减 × 频率加成 × 重要性加成 × 反馈加成**

- 时间衰减：指数衰减，每日自然遗忘

- 频率加成：对数增长，避免过热

- 重要性：永久固定权重

- 反馈分：用户/Agent 训练自进化

## 9\. 领域适配器标准契约（最终Trait）

所有领域必须实现，内核永久不变

```rust

pub trait DomainParser: Send + Sync {
    fn domain_name(&self) -> &str;
    fn split_semantic_chunks(&self, raw: &str, ctx: &ParseContext) -> Vec<SemanticChunk>;
    fn enrich_chunk(&self, chunk: &SemanticChunk) -> (String, Value);
    fn extract_edges(&self, nodes: &[MemoryNode]) -> Vec<RefEdge>;
    fn incremental_parse(&self, old: &MemoryNode, diff: &str) -> Result<MemoryNode>;
    fn format_context(&self, node: &MemoryNode, mode: ContextMode) -> String;
    fn compress_nodes(&self, nodes: &[MemoryNode]) -> MemoryNode;
    fn rerank_nodes(&self, query: &str, candidates: &mut Vec<ScoredNode>);
}

pub trait DomainTokenizer: Send + Sync {
    fn tokenize(&self, text: &str) -> Vec<String>;
    fn stop_words(&self) -> &[&str];
    fn expand_query(&self, query: &str) -> Vec<String>;
}
    
```

## 10\. 最终架构守恒原则（永久不变）

1. **内核永远领域无关**，所有业务差异在适配器

2. **唯一持久层 PostgreSQL**，无其他存储分支

3. **结构化树\+词法\+图谱三位一体**，永不单依赖向量

4. **冷热是逻辑状态，不是物理分层**

5. **写入幂等、检索可解释、生命周期自闭环**

6. **轻量优先、可扩展不增重**

> （注：部分内容可能由 AI 生成）

### 启动
```
make serve-debug
```

### 调用模型 
```
make orch-run ORCH_MSG='你好'
```
### 停止
```
make serve-stop
```

# 整套六边形架构五层完整职责划分（入站适配器、应用层、领域层、出站适配器、基础设施‑公共层）
结合你当前 AI‑Agent 聊天引擎项目，每一层只干自己分内工作、禁止跨层插手别人职责，同时附带禁止事项、现实业务例子、边界红线，一次性区分清楚。
整体流向：
**入站适配器 → 入站装饰器（Trace、异常拦截、鉴权）→ 应用层(AppService) → 领域层(Domain) → 出站适配器 → 基础设施**

## 一、入站适配器（Inbound Adapter / 驱动适配器）
作用：**外部世界 → 程序内部请求DTO 的协议转换入口**
外部调用源：HTTP接口、WebSocket流式请求、CLI命令行、RPC、定时任务、前端回调
### 工作职责
1. 解析外层协议：解析http‑json、websocket二进制、命令行参数、请求头
2. 协议层面参数基础反序列化；把外部请求转为项目内部统一的请求DTO
3. 协议级别的校验：请求体大小、header、路由参数、格式是否合法
4. 调用端口接口（`ChatPort`、`SkillPort`），**绝不书写业务逻辑**
5. 将应用层返回的业务响应 DTO，再序列化打包成协议输出（json、流式chunk、命令行打印）
6. 处理网络层面异常：断开连接、超时、序列化失败

### 禁止做的事情
- 不要写消息长度校验、敏感词过滤这类业务校验（交给应用层）
- 不要直接调用领域引擎
- 不要硬编码错误提示文案
- 不要操作数据库、缓存

### 举例
HttpAdapter 只负责：接收post请求 → 生成 OrchestrateRequest → 调用 chat_port.orchestrate() → 将 OrchestrateResponse 返回给前端

---

## 二、入站端口装饰器（中间件，属于入站侧包装层，独立于业务AppService）
也就是你现在的 `TraceAppService`、后续可以新增鉴权装饰器、异常捕获装饰器、限流装饰器
### 工作职责
1. 横切通用公共逻辑，对所有业务用例统一生效
2. Trace装饰器：创建trace_id、记录入口出口、推送请求生命周期事件
3. 全局异常装饰器：统一捕获所有上层抛出的领域错误
4. 接口限流、用户权限拦截、日志打印
5. 只是转发端口调用，**不参与业务编排**

> 装饰器包裹真实的 AppService，遵循装饰器模式，多个业务服务可以复用同一套切面

---

## 三、应用层 Application Service（业务用例层，你的 AppService1 / AppService2）
一句话定位：**业务流程总指挥，编排者，连接适配器与领域层**
只关心「业务步骤顺序」，不实现底层核心算法。
### 工作职责
1. **业务参数校验与清洗**
   每个业务独立规则：聊天消息长度、技能参数、会话id合法性、敏感字符过滤、用户id兜底初始化
2. 依赖准备
   加载缓存、会话内存、知识库客户端、事件总线、日志组件
3. 编排多个领域组件（最核心职责）
   顺序调度记忆读取、专家匹配、技能加载、LLM引擎；规定先干什么、后干什么、失败之后怎么走
4. 捕获领域层抛出的 DomainError，调用全局公共异常翻译工具，转为业务DTO
> 错误映射表抽离成公共函数，多个AppService共用，没有重复代码
5. 领域原始结构体 → 对外响应DTO组装，填充耗时、token、状态、附属信息
6. 触发前置、后置领域事件；会话记录、聊天记录持久化
7. 编排多存储操作、控制业务层面事务、执行回滚逻辑
8. 组装、打包入参交给领域引擎，接收领域结果

### 禁止事项
1. 禁止编写核心算法、prompt拼接、专家调度底层逻辑（交给领域层）
2. 禁止处理HTTP、websocket这类协议细节
3. 禁止直接操作数据库API，只调用出站端口抽象接口

---

## 四、领域层 Domain（业务核心，整个项目最重要一层）
一句话定位：存放全部**纯业务规则、核心算法、业务实体、聚合、领域错误、领域事件枚举**，和外部基础设施完全解耦。
你的 `OrchestrationEngine`、专家调度器、技能执行器、Prompt构造器、会话聚合体全部属于领域层。
### 工作职责
1. 定义领域实体、聚合、结构体：专家对象、技能对象、会话上下文、LLM执行结果
2. 实现最核心的业务算法：plan‑act规划、多专家分发、工具调用、prompt组装、token统计
3. 抛出纯粹的领域错误 DomainError，只包含业务语义，不带前端文案、http状态码
4. 执行业务规则：上下文长度限制、专家路由策略、技能权限规则
5. 运行途中发出各类领域事件：`OnPlanCreated`、ExpertInvokeStart、TokenConsume
6. 只依赖抽象端口（例如追踪端口、事件总线抽象），**不依赖任何具体数据库、HTTP实现**

### 禁止事项
1. 禁止写日志打印、数据库读写、缓存操作
2. 禁止编写前端响应文案、错误提示
3. 禁止感知HTTP、WebSocket、命令行等外部入口
4. 禁止处理参数清洗、参数校验（应用层提前校验完毕再送入引擎）

> 理想状态：领域层代码可以单独抽出来，脱离web环境进行单元测试

---

## 五、出站适配器 Outbound Adapter / 驱动侧适配器
作用：**面向外部基础设施的实现层，实现领域层定义的抽象端口(Port)**
六边形架构经典：内层定义接口（Port），外层适配器完成具体实现
### 工作职责
1. 实现各类基础设施端口接口：`TraceStoragePort`、`ChatRecordPort`、`CachePort`、`KnowledgeBasePort`、EventBus
2. 封装第三方工具：数据库(Mysql‑适配器)、Redis缓存、LLM接口请求、文件存储、日志组件
3. 将领域层的实体结构体，转换成数据库数据表模型；数据库结果转回领域实体
4. 隔离第三方SDK，所有数据库、网络、缓存细节全部封装在此层

### 禁止事项
1. 不要书写业务规则
2. 不要修改业务数据、增加业务判断
3. 不要返回业务错误文案；IO异常向上抛出，交由上层处理

举例子
- `MysqlChatRecordAdapter`：实现存储聊天记录的端口
- `OpenAiLlmAdapter`：实现llm调用端口

---

## 六、基础设施层 Infrastructure（底层公共工具、通用组件）
存放和业务无关、项目通用底层工具
### 工作职责
1. 事件总线、线程池、uuid生成、时间工具、加解密、字符串工具
2. 全局日志、统一错误翻译函数、公共DTO
3. 配置读取、环境变量、连接池

---

# 极简层级执行链路对照（你的聊天请求完整路径）
1. HTTP入站适配器：解析json → 请求DTO
2. Trace装饰器：开启根Span
3. AppService应用层：参数校验、读取历史会话、编排流程
4. 调用领域编排引擎
5. 引擎运行，抛出计划、专家调用、token消耗等领域事件
6. 事件总线广播事件
7. TraceObserver、会话存储出站适配器消费事件，写入数据库
8. 引擎返回领域结果 → 应用层组装响应DTO
9. 回到HTTP适配器，序列化json返回前端

# 一句话记忆每层定位
1. **入站适配器**：外面的请求进门
2. **装饰器切面**：通用横切功能（追踪、限流）
3. **应用层**：流程总指挥，安排先做什么后做什么
4. **领域层**：核心业务算法与业务规则
5. **出站适配器**：对接数据库、缓存、大模型等外部资源
6. **基础设施层**：项目通用底层工具
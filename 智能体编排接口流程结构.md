# 智能体编排接口 · 流程结构

> 更新：2026-09-14。基于当前代码实测整理（六边形架构），所有引用为 `文件:行号`，行号会随迭代漂移，以函数名为准。
>
> **核心红线速览**：
> 1. 路由/匹配**只靠 tags 打分**，禁止再造关键词表（与路由同源零漂移）
> 2. `Subhuti` 用 `Arc` 共享，编排主链路**无锁**
> 3. `Subhuti::new(event_bus)` 只 1 参，LLM/藏经阁由 `set_llm()`/`set_sutra_library()` 后续注入
> 4. **任一步失败 → 整体 `success=false`**（判据 `has_failed_steps()`）
> 5. `trace_summaries` 表**无 token_usage 列**，token 要从 `trace_spans.tokens` 求和
> 6. 编排出口沉淀必须 `await`（`consolidate()` → `auto_precipitate` → `precipitate_session_async`）

---

## 1. 总览图

```
                 ┌─────────────────────────────────────────────┐
                 │                入口层（Adapter）              │
                 │                                             │
   HTTP (axum)   │  POST /subhuti/api/v1/orchestrate           │
   ─────────────►│  ├─ Accept 含 text/event-stream → SSE       │
                 │  └─ 否则 → 一次性 JSON                       │
                 │                                             │
   MCP (stdio)   │  subhuti_chat / subhuti_memory / ...        │
   ─────────────►│  （JSON-RPC 2024-11-05，300s 工具超时）       │
                 │                                             │
   CLI           │  api orchestrate → 复用 HTTP 入口            │
   ─────────────►│  serve / mcp / doctor / log-stream          │
                 └──────────────────┬──────────────────────────┘
                                    ▼
                 ┌─────────────────────────────────────────────┐
                 │   应用服务 OrchestrationService (ChatPort)    │
                 │   orchestrate() / orchestrate_stream()      │
                 └──────────────────┬──────────────────────────┘
                                    ▼
                 ┌─────────────────────────────────────────────┐
                 │   编排核心 Orchestrator（subhuti-core）        │
                 │   ① tags 相关性打分选专家（唯一路由真相）        │
                 │   ② 单域快速路径 / 多域 Planner / 粘性兜底      │
                 │   ③ plan_and_execute（L1→L2→L3 自适应）       │
                 └──────────────────┬──────────────────────────┘
                                    ▼
                 ┌─────────────────────────────────────────────┐
                 │   专家执行（ExpertAgent trait）               │
                 │   LLM 装饰链：provider→ContextLimit→Retry    │
                 │              →CachedLLM(仅debug)            │
                 └──────────────────┬──────────────────────────┘
                                    ▼
                 ┌─────────────────────────────────────────────┐
                 │   出口：结果 + 记忆沉淀 + 执行记录              │
                 │   consolidate() → 藏经阁 / record_execution  │
                 └─────────────────────────────────────────────┘
```

---

## 2. 入口层

### 2.1 HTTP（axum）

| 环节 | 位置 |
|---|---|
| 服务启动 `start_server()` | `src/adapter/inbound/http/server.rs:21` |
| 路由自动注册（inventory 模式） | `src/adapter/inbound/http/route_adapter.rs:73`（`RouteEntry`）→ `build_router()` `:101` |
| 请求 DTO `OrchestrateRequest` | `src/adapter/inbound/http/adapters.rs:132` |
| **SSE 分流** `orchestrate_handler()` | `adapters.rs:372`：按 `Accept` 头分流（`wants_event_stream()` `:246`） |
| 非流式 `orchestrate_json()` | `adapters.rs:255` |
| 流式 `orchestrate_sse()` | `adapters.rs:332` |
| 挂载点 | `POST /subhuti/api/v1/orchestrate`（`adapters.rs:384`） |

中间件：`CorsLayer` + `RequestLogLayer` + `TraceIdLayer`。

**SSE 事件流**（`stream_to_sse()` `adapters.rs:162`，每个事件装饰 `session_id`）：

| 事件 | 行号 | 语义 |
|---|---|---|
| `start` | `:183` | 编排开始 |
| `step` | `:187` | 阶段步骤（phase=route/think/tool/retrieve） |
| `ask` | `:203` | 主动提问（阻塞等待用户回答） |
| `data` | `:215` | 中间数据 |
| `done` | `:223` | 完成，`meta` 含 `chain`/`duration_ms`/`tokens_used`/`llm_calls` |
| `error` | `:235` | 失败 |

### 2.2 MCP（stdio JSON-RPC）

- 实现：`src/adapter/inbound/mcp.rs`，协议版本 **2024-11-05**；`run()`（`:81`）用**专用 OS 线程写 stdout**（避免 tokio JoinHandle panic 传播）。
- 工具超时：`DEFAULT_TOOL_TIMEOUT_SECS = 300`（`:62`），可被 `SUBHUTI_MCP_TOOL_TIMEOUT_SECS` 覆盖——包住 LLM 降级可能无界耗时的调用。
- 工具清单（`list_tools_result()` `:427`）：

| 工具 | 位置 | 说明 |
|---|---|---|
| `subhuti_chat` | `:513` | 内部走 `orchestrate_stream` + `ProgressNotifier`（→ `notifications/progress`） |
| `subhuti_list_experts` | — | 专家列表 |
| `subhuti_match_expert` | — | 专家匹配 |
| `subhuti_skill_list` / `subhuti_skill_run` | — | 技能 |
| `subhuti_memory` | `:723` | `recall`/`write`/`stats`/`collections` 四 action；write 复用 `is_worth_remembering` 去噪（`:758`） |

- 握手注入 `SERVER_INSTRUCTIONS`（`:34`，进 `InitializeResult` `:422`）：触发词约定见 §8。

### 2.3 CLI

`src/adapter/inbound/cli/mod.rs:19`：`serve`（起 HTTP）、`mcp`（起 MCP server）、`api orchestrate`（HTTP 客户端，`cli/api.rs:127`，**复用 HTTP 入口、不直连编排核心**）、`doctor`/`log-stream`（诊断，不进主链路）。

**三入口汇入点**：HTTP/MCP → 应用服务 `OrchestrationService`（实现 `ChatPort`）；CLI → HTTP 间接汇入。

---

## 3. 编排主链路

### 3.1 应用服务层

`src/application/orchestration_service.rs` → `OrchestrationService`：

- `orchestrate()`（`:118`）：非流式。完成后调 `memory_consolidator.consolidate(...)`（`:226`，**须 await**）→ `record_execution`（`:233`）。
- `orchestrate_stream()`（`:266`）：流式。每请求订阅 `ProgressEventBridge`（`:307`），`tokio::select` 合并进度流与引擎结果（`:419`）；`Done.meta` 透出 `chain`/`duration_ms`/`tokens_used`/`llm_calls`（`:573`）。
- `domain_of()`（`:93`）：按专家链末位推断领域。

### 3.2 域识别与路由（`crates/subhuti-core/src/orchestrator/mod.rs`）

`Orchestrator`（`:273`，无锁 `RwLock` 内部状态）：

```
dispatch()（:445）
 ├─ 请求显式指定 expert_id → 直达
 ├─ graph_name 别名 → 直达
 └─ dispatch_without_graph()（:650）★判决点
      ├─ relevant_actors()（:616）：tags 命中数降序 ★路由唯一真相
      ├─ 命中 1 个  → 单域快速路径（:662）
      ├─ 命中 ≥2 个 → 多域 dispatch_with_plan()（:668，走 Planner）
      └─ 命中 0 个  → 会话粘性兜底（:679）
           └─ looks_like_continuation()（:341）：承接表达（"继续/那…"）
              → 延续 last_expert_id；否则 fallback 域边界提示（:573）
```

**红线**：路由/匹配只有一套真相——**tags 相关性打分**（`select_actor_by_relevance` `:519`）。`composition_root.rs:275` 注释明确：Dispatch/Execution 规则表已删除，禁止再造关键词表。

路由确定处（`dispatch_via_actor()` `:832`）：记录 `last_expert_id`（`:853`），发 `AgentMatched`/`ChainSelected` 事件，专家 `perform` 成败决定整体 `success`。

### 3.3 规划与执行（planner + adaptive）

**Planner**（`crates/subhuti-core/src/orchestrator/planner.rs`）：

| 环节 | 位置 |
|---|---|
| `generate_plan()`（含 PLACEHOLDER 步骤过滤） | `:371`（过滤 `:255`） |
| `generate_expert_plan()` | `:442` |
| `chat_and_parse_plan()`：解析失败自动纠错重试一次 | `:319` |
| `parse_plan_or_ask()`：解析不出 → 主动提问 | `:198` |
| `execute_plan()` 串行执行 | `:511` |
| `dispatch_with_plan()`：串行执行 + `all_ok` 聚合 | `mod.rs:718` |
| `has_failed_steps()`：**任一步失败即整体失败** | `planner.rs:108` |

**plan_and_execute 通用实现**（`src/domain/traits.rs:257`，专家域层默认）：

```
generate_plan（:296）
 ├─ 规划失败 → 降级单步 chat（:308）
 ├─ 主动提问 → 阻塞等待，MAX_ASK_ROUNDS=2（:340）
 ├─ 空计划   → 退化为直答（:398，不算失败）
 └─ execute_plan_adaptive（:488，assemble_fallback :437）
      └─ has_failed_steps 判决（:530）：任一步失败 → DomainError::ExecutionError
```

**自适应执行**（`crates/subhuti-core/src/orchestrator/adaptive.rs`）：

- L1 常态执行 → 失败进 L2 反馈重试（`max_retry=2`）→ 仍失败进 L3 降级（`StepFallback`，`max_fallback_iters=3`）。
- `Precondition` 短路直接跳过 L2/L3（`:174`）。
- L3 降级实现：`src/domain/tool_fallback.rs` → `DomainLlmFallback::execute()`（`:281`），带递归防护提示词（`:305`），最多 3 轮工具迭代（`:347`）；工具集 `file_read/write/list/search` + `command_run`（`build_tools()` `:25`）。
- **已知残留**：L3 降级存在"LLM 返回文本即成功"的宽松语义，尚未收紧。

### 3.4 专家调度

- `ExpertAgent` trait（`orchestrator/mod.rs:158`）：`tags()`/`skills()`/`run()`。
- `AgentRegistry`（`:950`）：`register`/`find_matching_experts`/`get_by_id`。
- `Subhuti` 引擎（`crates/subhuti-core/src/engine.rs:20`）：
  - `Subhuti::new(event_bus)` **一参构造**（`:43`）；`set_llm()`（`:79`）/`set_sutra_library()`（`:84`）注入。
  - `register_orchestrator_expert()`（`:105`）/`register_actor()`（`:110`）。
  - `dispatch_with_context()`（`:122`）：无锁并发，编译期 `Send+Sync` 断言（`:217`）。
- 注册注入点：`src/adapter/outbound/subhuti_framework_initializer.rs` → `register_expert()`（`:382`，注册 `ExpertAgent` + 竞标 `Actor` + 注入 session_manager）。
- 唯一装配点：`CompositionRoot::build()`（`src/application/composition_root.rs:70`）。

---

## 4. LLM 装配链（装饰器，顺序固定）

```
provider client → ContextLimitLLM → RetryLLM → (CachedLLM，仅 debug)
```

装配点：`subhuti_framework_initializer.rs:223`（Retry 包 ContextLimit）、`:267`（Cached）、`:280`（`set_llm`）。

| 装饰器 | 文件 | 要点 |
|---|---|---|
| **ContextLimitLLM** | `crates/subhuti-infra/src/llm/limits.rs` | 裁剪上下文：最后一条恒保留、System 尽量全留、最近优先回填、孤立 Tool 丢弃（`trim_messages` `:115`）。`max_messages=20`、`max_chars=32000`（`:46`） |
| **RetryLLM** | `llm/retry.rs` | 重试条件：**408/429/5xx 重试，4xx 不重试**（`decide()` `:112`）；指数退避 + 429 保底 1s + 抖动（`:139`）。`max_attempts=3`、`timeout_secs=120`（`:47`）。流式**仅在未吐 delta 时重试**（`:373`）。env：`SUBHUTI_LLM_RETRY`/`MAX_ATTEMPTS`/`TIMEOUT_SECS` |
| **CachedLLM** | `llm/cached_llm.rs` | LRU 100 条，SHA-256 key（`:200`）；**仅 debug + `SUBHUTI_LLM_CACHE=1`**；流式不缓存（`:372`）；`chat_counted()` 命中返回 `(cached, None)`——**缓存命中 token 用量诚实为 0** |

**token 统计**：一律走 `chat_counted()`/`chat_streaming_counted()`；缓存命中返 None。

**⚠️ 扩 `LLM` trait 时的坑**：三个装饰器（ContextLimit/Retry/Cached）必须**显式转发**新方法，漏一个链路就断。

---

## 5. 事件与可观测性

### 5.1 事件总线

`crates/subhuti-core/src/event/bus.rs` → `EventBus`（`:58`，broadcast + handlers）：

- `publish()`（`:152`）：`tokio::spawn` fire-and-forget，**Handler 不阻塞主链路**。
- `emit_with_trace()`（`:193`）。
- 事件类型：`AgentEventData` 枚举（`event/types.rs:98`）——`UserMessage`/`AgentMatched`/`ChainSelected`/`AgentStarted`/`AgentCompleted`/`AgentFailed`/`LLMCalling`/`LLMResponded`/`ToolCalling`/`ToolResponded`/`MemoryRetrieved` 等。

### 5.2 桥接层（`src/adapter/outbound/event_bridge.rs`）

| 桥 | 位置 | 作用 |
|---|---|---|
| `TraceEventBridge` | `:24`（`convert()` `:34`） | 全局注册，框架事件 → `SpanData`（无 trace_id 返回 None） |
| `ProgressEventBridge` | `:436` | **per-request 订阅**，filter=五种事件；`LLMResponded` 累加 token/调用数（`:546`）；`to_step()`（`:469`）渲染 phase=route/think/tool/retrieve |

### 5.3 追踪持久化

`crates/subhuti-infra/src/trace_store.rs` → `SqliteTraceStore`（`:76`），独立 OS 线程 worker（`:531`）：

- 表结构（`init_schema()` `:182`）：
  - `trace_summaries`（`:184`）：trace_id/user_id/session_id/message/output/error/status/duration_ms/chain_name/expert_chain——**⚠️ 无 `token_usage` 列**。
  - `trace_spans`（`:201`）：**含 `tokens` 列** → token 统计从 spans 求和。
  - `trace_fn_calls` / `trace_fn_logs`。
- WAL + `busy_timeout`（`:574`）；HTTP 与 MCP 两进程汇聚**同一个 `traces.sqlite`**。
- 日志：HTTP 落 `./logs/*.log`（`server.rs:33`）；MCP 落 `./logs/mcp.log`（`mcp.rs:808`）。

---

## 6. 出口：记忆沉淀与执行记录

```
编排完成（orchestrate / orchestrate_stream 出口）
    │
    ├─► MemoryConsolidator::consolidate()          （src/application/memory_consolidation.rs:69）
    │     ├─ extract_facts()（:104）：LLM 提炼（消息≥8字才调）
    │     │    + 规则兜底 rule_extract（:224）
    │     │    + is_worth_remembering 去噪（:276）
    │     │    + grounded_in 2-gram 锚定（≥20% 重叠，:138）
    │     │    + 去重、限 5 条
    │     └─ sutra.auto_precipitate()（→ precipitate_session_async）★须 await
    │
    └─► record_execution()（memory_consolidation.rs:46）
          已上移编排层，对所有域一视同仁（此前仅 Blender 专家调用）
```

- 沉淀**失败静默**——记忆是增强项，不影响编排成功。
- MCP `subhuti_chat` 同样经 `orchestrate_stream` 走此链路（隐式召回在专家内：`library_retrieve()`，如 `src/domain/experts/rust_expert.rs:275`）。

---

## 7. 成败语义（一句话）

> **任一 plan step 失败 → 整体 `success=false`**；空计划不算失败。
> 判决点：`planner.rs:108` `has_failed_steps()` 与 `dispatch_with_plan` 的 `all_ok` 同口径；领域层判决在 `traits.rs:530`（`plan_and_execute`）。
> SSE 的 `Done.meta.success` 即据此透出。

---

## 8. MCP 握手约定（SERVER_INSTRUCTIONS）

`src/adapter/inbound/mcp.rs:34`，注入 `InitializeResult`（跨客户端生效，改这里并 `cargo build --bin subhuti`）：

- **显式写入触发词**：用户消息出现 **保存 / 记住 / 知识库 / 藏经阁** + 具体内容 → 调 `subhuti_memory` `action=write`。
- **回忆触发**：问过去的事/偏好/约定 → 先 `subhuti_memory` `action=recall`。
- **domain 取值**：`blender` / `rust` / `general`。
- **写入质量**：正文须自带可召回关键词（召回靠 jieba 分词重叠，非语义向量）；意图句（提问/空泛话题）不写。

---

## 附：常见坑位对照

| 现象 | 真相 | 处置 |
|---|---|---|
| MCP 报 401 / chat 秒挂 | `ZHIPU_API_KEY` 只从 env 取，客户端拉起子进程不设 cwd | `mcp.json` 配 `cwd` 项目根 + env 三键（见藏经阁文档 §7） |
| token 统计对不上 | `trace_summaries` 无 token 列 | 从 `trace_spans.tokens` 求和 |
| 新增 LLM 方法后行为异常 | 装饰器未显式转发 | 三个装饰器逐一补转发 |
| 审计 dead_code 误报 | 内部 crate 的 `pub` 项不报 dead_code | 用 Grep 查真实调用点 |
| `serve` 端口疑云 | 实测默认监听 `127.0.0.1:8618`（与前端约定 8615 不一致，待核） | `lsof` + curl 探活，本地 curl 加 `--noproxy '*'` |

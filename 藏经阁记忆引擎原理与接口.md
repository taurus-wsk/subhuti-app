# 藏经阁记忆引擎 · 原理与接口

> 更新：2026-09-14。基于当前代码实测整理，所有引用为 `文件:行号`，行号会随迭代漂移，以函数名为准。
> 核心实现：`crates/subhuti-infra/src/sutra_library/`；判据与出口：`src/application/memory_consolidation.rs`、`src/adapter/inbound/mcp.rs`。
>
> **核心红线速览**：
> 1. hydrate 循环**不要**调 `register_chunk`（长文档抽取卡十几秒）
> 2. **边的真相 = `graph_entity_chunks` + 当前建边规则**（启动重建即自愈），不从 `graph_edges` 回灌
> 3. **无向边三约束**：端点归一化、kind 只用 `Manual`/`Learned`、(min,max) 去重——违反则边数虚高 4 倍
> 4. 判据必须覆盖**所有落库入口**（`is_worth_remembering` 为 `pub(crate)`，MCP write 与自动沉淀同源）
> 5. **召回靠 jieba 分词词项重叠、非向量语义**——记忆正文必须自带关键词
> 6. `recall(top_k)` 的 N 是**最终条数**（09-14 修复截断）
> 7. 手工删库后 Tantivy 不会自动同步（hydrate 已加 `num_docs()` 比对 + `clear()` 重建）

---

## 1. 总览图

```
写入入口（4 条）                      召回链路（5 阶段）
────────────────                    ────────────────
① 编排沉淀（自动）                    query
   MemoryConsolidator.consolidate      │
   → auto_precipitate                  ▼
   → precipitate_session_async      ① BASE 全文检索（Tantivy/jieba，BM25）
② MCP 直写（显式触发词）                │
   subhuti_memory(write)             ② SPACE 静态空间扩散（目录距离）
   → auto_precipitate                  │
③ 冷启动种子（SUBHUTI_DISABLE_SEED    ③ GRAPH1 一阶 BFS（实体图）
   可关；make_plain_node 直存）          │
④ 领域切片 write_node                ④ GRAPH2 二阶（默认关）
      │                                │
      ▼                                ▼
┌─────────────────────────────────────────────────────┐
│  fuse_by_source 加权融合（rank 归一化）                 │
│  BASE 1.0 / SPACE 0.5 / GRAPH1 0.3 / GRAPH2 0.15    │
│  → top_k 最终截断 → 输出侧过滤（脏项/空内容）            │
└─────────────────────────────────────────────────────┘
      │
      ▼
┌────────────────── 存储层 ────────────────────────────┐
│ 内存：MemoryStorage（nodes/BM25/collections/edges）    │
│ 持久化：SQLite 降级（sutra_library.sqlite，WAL）或 PG  │
│ 全文：Tantivy（SUBHUTI_DATA_DIR/tantivy_index）       │
│ 图谱：petgraph EntityGraph（+GraphPersistence 双写）   │
└─────────────────────────────────────────────────────┘
```

---

## 2. 存储层

### 2.1 三层拓扑

| 层 | 实现 | 位置 |
|---|---|---|
| 内存高速层 | `MemoryStorage`（nodes/BM25/collections/path_index/edges/session_memory） | `sutra_library/storage.rs:145` |
| 持久化后端 | `PersistencePort`：有 PG 池走 PG（`PgStorage` `storage.rs:315`），否则**降级 SQLite**（`SqliteStorage` `persistence.rs:243`） | `persistence.rs:44`，选择逻辑 `mod.rs:46` |
| 全文索引 | `TantivyIndex`，落 `SUBHUTI_DATA_DIR/tantivy_index` | `recall/tantivy_index.rs:143`，路径 `mod.rs:107` |
| 实体图谱 | petgraph `UnGraph`（`EntityGraph`），SQLite 路径经 `GraphPersistence` 双写 | `recall/graph.rs:112`，注入 `mod.rs:142` |

### 2.2 SQLite 关键表（`persistence.rs:369` 建表）

| 表 | 说明 |
|---|---|
| `memory_nodes` | 记忆切片：node_id/content_hash/domain/path/title/summary/content/feedback_score 等（`:381`） |
| `memory_collections` | 集合；同名同域幂等收敛，绝不删仍被 nodes 引用的行（`:493`） |
| `graph_entity_chunks` | **实体↔切片反向索引，建边真相来源**（`:460`）；⚠️ `chunk_id` 就是 node_id |
| `graph_edges` | (from,to,edge_kind,weight)；kind 仅 `Manual`/`Learned`（`:452`） |
| `graph_entity_feedback` | 实体反馈分（`:466`） |
| `domain_kb` + `kb_chunk` | 知识库表（`:427`） |

SQLite 打开即 `create_if_missing` + **WAL** + `busy_timeout 5s`（`:266`），保证 HTTP/MCP 多进程并发不 `SQLITE_BUSY`。**WAL 勿关**。

### 2.3 Tantivy 索引

- Schema（`tantivy_index.rs:228`）：`node_id/collection_id/domain/...` 用 raw 分词器（精确过滤）；`title/content/summary` 用**自研 jieba**（`TokenizeMode::Search` + hmm，`:63`）。
- 检索：`search_with_filters()`（`:465`）——查询侧 jieba 分词→去停用词→各词在三个字段上做 **OR（Should）**（`:587`）；过滤条件 Must 拼 BooleanQuery（`:528`）。
- 写入：`index_node()`（`:385`）先 `delete_term(node_id)` 再 add——幂等覆盖；`commit()` 写锁（`:432`）；`clear()`（`:443`）全量删除。
- **召回原理边界**：靠分词后的**字面 term 重叠**（OR 语义）。零字面重叠的纯语义查询搜不到（例：「怎么记录运行信息」搜不到「日志库选 tracing」）→ **写记忆时正文必须含可召回关键词**。缓解：`QueryAnalyzer` 同义词扩展（`recall/query.rs:48`，如 api↔接口、pg↔PostgreSQL），但只补查询侧。

---

## 3. 写入链路

### 3.1 四条入口

| 入口 | 链路 | 位置 |
|---|---|---|
| ① 编排沉淀（自动） | `MemoryConsolidator::consolidate` → `sutra.auto_precipitate`（engine.rs:751）→ `precipitate_session_async`（engine.rs:682） | `memory_consolidation.rs:69`；编排出口调用须 **await** |
| ② MCP 直写 | `subhuti_memory(action="write")` → 复用 `is_worth_remembering`（mcp.rs:758）→ `auto_precipitate` | `mcp.rs:746` |
| ③ 冷启动种子 | `seed_domain_knowledge` → `make_plain_node` **直存原文**（不走领域切片，避免长文灾难性回溯） | `engine.rs:925`/`:969`；装配 `composition_root.rs:362`，`SUBHUTI_DISABLE_SEED=1` 可关（`:355`） |
| ④ 领域切片 | `write_node`（先查 content_hash 幂等） | `engine.rs:279` |

### 3.2 幂等：content_hash = SHA-256（engine.rs:1119）

- `write_node`：内存 `content_hash + collection_id` 命中即跳过（`:300`）。
- `precipitate_session_async`：`known` 集合跳过已存在（`:689`）。
- `seed_domain_knowledge`：跳过已 seed（`:937`）。
- 建边：`upsert_edge` 存在则只提升权重（`graph.rs:508`）。

### 3.3 去噪判据（两级）

**`is_worth_remembering`**（`memory_consolidation.rs:276`，`pub(crate)` 供 MCP write 复用，注释强调**手动 write 与自动沉淀判据必须同源**）：

- 长度 `(4..=200)`；纯寒暄 NOISE 表；纯疑问句（`？`/`?` 结尾）；命中 `is_intent_sentence` 即拒。

**`is_intent_sentence` 六段意图拦截**（`memory_consolidation.rs:324`）——拦"提问/空泛话题"类垃圾：

| 段 | 判据 | 备注 |
|---|---|---|
| ① | 句首意图动词（关注/需要/希望/想要/想了解/如何/怎么…） | `starts_with` |
| ② | 句中意图组合词（需要了解/希望获取/有明确需求…） | `contains` |
| ③ | 「用户/我」+ 泛意图动词（用户关注/我需要查看…） | 第三人称复述 |
| ④ | ≤25 字且以 介绍/描述/说明/应用/作用/概述 收尾（话题标题） | **尾巴前豁免**：stem 含「用/是/为/必须」则放过（「提交信息用中文描述」是真事实）。⚠️ 豁免只看尾巴**之前**——「应用」「作用」自身含「用」，整句 contains 会漏判 |
| ⑤ | **无锚点** + ≤25 字 + 含通用谓语（支持/用于/包括/分为/属于/涉及…） | 锚点表：用户/我/**本项目**/我们/偏好/默认/固定/配置/约定/记住/注意。⚠️ 锚点**不能有裸「项目」**——「项目涉及 Rust 编程」曾靠它逃逸落库（09-14 实测修复） |
| ⑥ | 动作片段前缀（了解/探索/学习/研究/查看） | |

测试回归：`intent_sentences_are_rejected`（`:459`）、`real_facts_survive_intent_filter`（`:497`）。

### 3.4 提炼链（自动沉淀）

`extract_facts`（`memory_consolidation.rs:104`）：

1. LLM 提炼（消息 ≥8 字才调，提示词要求只提炼**用户自述事实**、不记提问）；
2. 规则兜底 `rule_extract`（`:224`，含「我用/固定/版本是」等标记或含数字）；
3. `is_worth_remembering` 去噪 → `grounded_in` 2-gram 字面锚定（≥20% 重叠，`:138`，防 LLM 幻觉漂移）；
4. 去重、限 5 条。
5. 全程失败静默——记忆是增强项，不影响编排成功。

---

## 4. 召回链路（重点，5 阶段）

主入口：`SutraLibraryPort::library_retrieve`（`engine.rs:1216`，String 层负责 top_k 截断与输出过滤）；管线：`recall/pipeline.rs:42`。

### 4.1 ① BASE 全文检索（pipeline.rs:54）

- `base_search` → Tantivy `search_with_filters`，BASE 分 = Tantivy 原生 BM25。
- 查询扩展：`QueryAnalyzer` 产出的同义/别名扩展逐一检索，按 chunk_id 取 **max base_score** 合并（`:56`），截断 `base_top_k`（默认 8）。
- 无 Tantivy 时回退内存 BM25 + 关键词（`base_search.rs:61`）。

### 4.2 ② SPACE 静态空间通路（pipeline.rs:102，space.rs）

- 仅按**物理目录树**扩散：同 collection=距离1（bonus 0.15）、同 domain=距离2（bonus 0.05）（`space.rs:97,121`）。
- **不驱动图谱扩散**；`root_entity_set` 全程固定（pipeline `:81`）。
- 排序按与 seed 实体的反馈加权重合度（`sort_by_entity_overlap` `:133`）。

### 4.3 ③ GRAPH1 一阶 BFS（pipeline.rs:114，graph.rs:966）

- seed 切片提实体 → petgraph BFS（`get_neighbors` `:658`，`max_depth` 默认 2）。
- 算分：`edge_base = weight × (Learned? 0.4 : 1.0) × 0.7^depth`（`graph.rs:999`）；`bonus_score ≤ 0.001` 丢弃（`:1007`）；每个 chunk 只保留最大分（`:1031`）。

### 4.4 ④ GRAPH2 二阶（可选，pipeline.rs:124）

- 默认关（`graph_second_pass_from_space=false`，`config.rs:45`）；开启时把 space 切片再投喂图谱，`score_scale = 0.65`。

### 4.5 ⑤ fuse_by_source 加权融合（recall/scoring.rs:52）

- 各通路**独立 rank 归一化**：第 1 名=1.0、末名=0.0 → 缩放到 `[0, weight]`（`:79`）。
- **权重**：`BASE 1.0 / SPACE 0.5 / GRAPH1 0.3 / GRAPH2 0.15`（`:30`）。设计动机：避免弱通路的第一名恒 1.0 压过 BASE 的精确命中。
- **各通路独立算分取 max**（`:89`）：多通路命中不丢证据（测试 `graph_expansion_never_outranks_base_match` `:187`）。

### 4.6 top_k 最终截断（09-14 修复）

- 旧 bug：base/graph 两路各自取数（`graph_max_global_result=N*2`）后**从不截断** → `top_k=1` 返回 2 条。
- 修法：String 层遍历候选 `if shown >= top_k { break; }`（`engine.rs:1241`）。测试 `top_k_caps_final_result_count`（`engine.rs:1576`）。
- 缓存**实际返回**的 chunk_ids 到 `last_retrieve_cache`/`last_retrieve_recent`，供 `record_execution` 使用（`:1299`）。
- ⚠️ 测试须走 `SutraLibraryPort` trait——inherent 同名方法会遮蔽 trait。

### 4.7 输出侧过滤（engine.rs:1237）

- `total = base_score + bonus_score`，`total <= 1e-6` 过滤（归一化末位 0 分脏项）。
- 内容全空节点（title/summary/content 全空）过滤。
- 无标题节点用正文前 24 字做展示标题；优先 summary，否则截正文 300 字。

---

## 5. 图引擎

### 5.1 建边

- `link_cooccurrence`（`graph.rs:483`）：单条记忆内共现实体两两建 `Manual` 边（weight=0.3），O(n²) 截断 `MAX_COOC=8`。
- **碎片过滤** `select_cooccur_entities`（`:462`）：实体 ≥3 字；剔除被同批更长实体包含的碎片，只留最长形式（「块职」并入「模块职责」）。节点自身 UUID 不参与。
- `add_learned_edge` 遇已有 `Manual` 边跳过（`:578`）；`Learned` 边按天衰减 0.9（24h 后台任务 `mod.rs:152`），≤0.05 删边（`graph.rs:805`）。

### 5.2 无向边三约束（违反 → 边数虚高 4 倍）

1. **端点归一化** `normalize_edge_endpoints`（`persistence.rs:30`）：按字典序排序 (from,to)，(A,B)/(B,A) 同落一行。
2. **kind 只用 `Manual`/`Learned`**（`:96`）。
3. **(min,max) 去重**：落盘 `persist_graph` 时去重（`engine.rs:886`）。

### 5.3 边的真相与重建

- **`load_persisted_data`（`graph.rs:228`）是重建语义**：基线传空 map；边由 `chunk_to_entities` 按当前建边规则**派生**，丢弃库里旧规则生成的邻接表——规则收紧后启动重建即自愈（测试 `hydrate_derives_edges_from_entity_index` `:1268`）。
- `GraphPersistence` 窄接口仅 6 方法（`graph.rs:35`）：load_all/write_edge/delete_edge/write_entity_chunk/write_entity_feedback/batch_delete_edges；PG/SQLite 双实现（SQLite 端到端验证过，PG 路径未真机验证）；无后端时图谱退化为纯内存（`:27`）。

### 5.4 record_execution（编排层）

- `MemoryConsolidator::record_execution`（`memory_consolidation.rs:46`）→ `sutra.record_execution`（engine `:1370`）。
- 优先 `last_retrieve_cache`（query_hash 精确命中），失败回退 5 分钟内 `last_retrieve_recent`。
- `chunk_used_in`（engine `:797`）：jieba 分词取实词，答案含其一即判"被用上"。

---

## 6. MCP 出口（全部出口只有 3 处）

| 出口 | 能力 | 位置 |
|---|---|---|
| `subhuti_memory` | `recall`（query/top_k 默认 5）/ `write`（复用 `is_worth_remembering` 拒意图句）/ `stats` / `collections` | `mcp.rs:723-786` |
| `subhuti_chat` | 隐式召回（专家内 `library_retrieve`）+ 编排出口自动沉淀 | `mcp.rs:513` |
| HTTP | `GET /sutra/stats`（`knowledge.rs:324`）+ `/knowledge-bases` 与 chunks CRUD（`:68-246`，无 PG 时 `SERVICE_UNAVAILABLE`） | `routes/knowledge.rs` |

触发词约定（`SERVER_INSTRUCTIONS` `mcp.rs:34`，跨客户端权威来源）：**保存 / 记住 / 知识库 / 藏经阁** + 内容 = 显式写入；问过去的事 → 先 recall。
**每个会话开始时至少 recall 一次**（如 `query="用户偏好 项目约定", top_k=3`）——recall 只读（SQLite+Tantivy，毫秒级、不调 LLM、无副作用），召空/失败静默继续（2026-09-14 加，instructions 与工具描述双处下发）。

---

## 7. 配置与运维

| 项 | 说明 |
|---|---|
| `SUBHUTI_DATA_DIR` | 统一数据目录（`data_dir.rs:35`），未设则 `~/.subhuti/data`。派生：`traces.sqlite`/`sutra_library.sqlite`（可 `SUBHUTI_SUTRA_SQLITE` 覆盖）/`sessions.sqlite`/`tantivy_index/` |
| `SUBHUTI_DISABLE_SEED=1` | 关闭冷启动种子注入（纯记忆引擎用途；不关会被混进召回且 stats 抖动） |
| hydrate 自愈 | `engine.hydrate`（engine.rs:616）回灌后比对 `tantivy.num_docs() != count` → `clear()` 全量重建（`:644`）。解决**手工删库后 Tantivy 幽灵文档**白占 top_k 的问题 |
| 后台任务 | `refresh_hotness`（6h）+ `decay_learned_edges(0.9)`（24h，`mod.rs:152`） |
| WorkBuddy 接入 | `~/.workbuddy/mcp.json`：`cwd`=项目根 + `env{SUBHUTI_DATA_DIR, SUBHUTI_DISABLE_SEED=1, ZHIPU_API_KEY}`。⚠️ cwd 必须在项目内（`.env` 靠 dotenvy 从 cwd 向上找，零兜底）；key 只认环境变量 |
| 探针测试 | 第二实例须**另指 `SUBHUTI_DATA_DIR`** 避开本会话 Tantivy 索引锁 |

---

## 8. 验收工具与基线（scripts/debug/）

| 脚本 | 用途 | 基线 |
|---|---|---|
| `mcp_memory_probe.py` | MCP 能力矩阵（工具面/幂等/边界防御/意图句拒写/跨进程持久化） | **18/18** |
| `recall_quality_probe.py` | 召回**质量**回归：正确答案须排第 1、对照查询须拒答、无零信息脏项 | **7/7** |
| `mcp_memory_engine_probe.py` | 藏经阁当纯记忆引擎：写入可靠/字面+同义召回/持久化/累积稳定性；独立库 `/tmp/subhuti-memtest`，真实库只读 | **42/42** |
| `cleanup_probe_data.py` | 清理探针残留+意图垃圾；`--self-test` 校验判据与 Rust 六段同源（基准 22 垃圾 + 14 事实） | — |
| `mcp_env_probe.py` | 接入自检（项目外 cwd 下各能力是否可达），`--no-llm` 免费 | — |
| 其他 | `graph_dualwrite_probe` / `dedup_graph_edges` / `sutra_verify` / `observability_snapshot` | — |

**验收基线**：workspace `cargo test` **184 绿**；清理后真实库 8 条（4 知识种子 + 3 真实配置 + 1 偏好）。

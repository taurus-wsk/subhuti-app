subhuti-app/                      # 项目根（Rust Workspace 根）
├── Cargo.toml                    # Workspace 成员声明 + 主二进制 crate 配置
├── 目录结构.md                    # 项目结构说明文档
├── src/                          # 主二进制 crate（应用层，按 DDD 分层）
│   ├── main.rs                   # 程序入口：DI 容器组装、服务启动、资源初始化
│   ├── subhuti_app.rs            # 备用入口
│   ├── lib.rs                    # 应用层库入口
│   ├── application/              # DDD 应用层：业务用例编排
│   │   ├── mod.rs
│   │   ├── app_service.rs        # 应用服务：协调领域对象完成完整用例
│   │   └── graph_integration.rs  # 图编排集成：图引擎与专家的桥接
│   ├── domain/                   # DDD 领域层：业务核心，纯逻辑无技术细节
│   │   ├── mod.rs
│   │   └── experts/              # 专家领域：实现 core 定义的 ExpertAgent trait
│   │       ├── mod.rs
│   │       └── blender.rs        # Blender 专家实现示例
│   └── infrastructure/           # DDD 基础设施适配层：业务级技术对接
│       ├── mod.rs
│       ├── config.rs             # 应用配置加载
│       ├── http/                 # Axum 控制器、路由、请求参数解析
│       │   ├── mod.rs
│       │   ├── server.rs         # HTTP 服务器配置与启动
│       │   ├── middleware.rs     # 中间件：日志、CORS、认证等
│       │   └── routes/           # 路由定义
│       │       ├── mod.rs
│       │       ├── orchestrate.rs# 编排路由
│       │       ├── experts.rs    # 专家管理路由
│       │       ├── skill.rs      # Skill 路由
│       │       ├── chat.rs       # 聊天路由
│       │       ├── traces.rs     # 追踪路由
│       │       ├── sessions.rs   # 会话路由
│       │       ├── palace.rs     # 记忆宫殿路由
│       │       ├── persona.rs    # 角色路由
│       │       ├── health.rs     # 健康检查路由
│       │       └── logs.rs       # 日志路由
│       └── cli/                  # 命令行子命令实现
│           ├── mod.rs
│           ├── api.rs            # API 命令
│           ├── db.rs             # 数据库命令
│           ├── doctor.rs         # 诊断命令
│           ├── flame.rs          # 性能分析命令
│           └── log_stream.rs     # 日志流命令
└── crates/                       # 内部库 crate 集合
    ├── subhuti-core/             # 核心框架层：纯抽象 + 通用运行时机制
    │   ├── Cargo.toml
    │   └── src/
    │       ├── lib.rs            # 核心框架入口，导出所有 trait 和类型
    │       ├── common/           # 通用基础类型、上下文、错误码、共享数据结构
    │       │   ├── mod.rs
    │       │   └── types.rs      # 通用类型定义
    │       ├── graph/            # 图编排内核：Graph/Node trait、通用调度器
    │       │   ├── mod.rs
    │       │   ├── engine.rs     # 图执行引擎
    │       │   ├── node.rs       # 节点定义
    │       │   ├── state.rs      # 图状态管理
    │       │   ├── checkpoint.rs # 检查点机制
    │       │   └── actor/        # 图节点 Actor 运行时
    │       │       ├── mod.rs
    │       │       ├── node_actor.rs    # 节点 Actor 实现
    │       │       ├── supervisor.rs    # 监督策略
    │       │       ├── scheduler.rs     # 调度器
    │       │       ├── message.rs       # 消息定义
    │       │       └── event_driven_actor.rs # 事件驱动 Actor
    │       ├── actor/            # Actor 运行时：消息邮箱、监督策略、生命周期管理
    │       ├── event/            # 事件总线：EventBus trait、通用事件分发器
    │       │   ├── mod.rs
    │       │   ├── bus.rs        # 事件总线实现
    │       │   ├── handler.rs    # 事件处理器
    │       │   ├── recorder.rs   # 事件记录器
    │       │   ├── replay.rs     # 事件重放
    │       │   └── types.rs      # 事件类型定义
    │       ├── runtime/          # 能力抽象：LLM/Tool/Memory/Session trait
    │       │   ├── mod.rs
    │       │   ├── llm/          # LLM 抽象
    │       │   │   └── mod.rs
    │       │   ├── tools.rs      # 工具抽象
    │       │   └── session.rs    # 会话抽象
    │       ├── observe/          # 观测和追踪接口定义
    │       │   ├── mod.rs
    │       │   ├── trace.rs      # Trace 定义
    │       │   └── session.rs    # Session 观测
    │       ├── vertical/         # 垂直场景能力抽象（资产、项目、工具、工作流）
    │       │   ├── mod.rs
    │       │   ├── asset.rs      # 资产库接口
    │       │   ├── project.rs    # 项目记忆接口
    │       │   ├── tool.rs       # 工具注册接口
    │       │   └── workflow.rs   # 工作流存储接口
    │       ├── memory/           # 记忆抽象接口
    │       │   └── mod.rs
    │       └── orchestrator/     # 编排器：专家调度、图引擎集成
    │           ├── mod.rs
    │           └── strategies/   # 路由策略
    │               └── mod.rs
    ├── subhuti-infra/            # 基础设施层：通用技术实现，无业务逻辑
    │   ├── Cargo.toml
    │   └── src/
    │       ├── lib.rs            # 基础设施入口
    │       ├── llm/              # LLM 客户端适配：OpenAI / Ollama / Doubao / Mock
    │       │   ├── mod.rs
    │       │   └── client.rs     # LLM 客户端实现
    │       ├── memory/           # 记忆实现：记忆宫殿、知识图谱、混合搜索
    │       │   ├── mod.rs
    │       │   ├── palace_graph.rs   # 记忆宫殿核心结构
    │       │   ├── knowledge_graph.rs # 知识图谱
    │       │   ├── hybrid_search.rs  # 混合搜索
    │       │   ├── fact_checker.rs   # 事实检查
    │       │   ├── convo_miner.rs    # 对话挖掘
    │       │   ├── embedding.rs      # 嵌入服务
    │       │   ├── entities.rs       # 实体提取
    │       │   ├── dynamics.rs       # 连接动态
    │       │   ├── dedup.rs          # 去重
    │       │   ├── short_term.rs     # 短期记忆
    │       │   ├── long_term.rs      # 长期记忆
    │       │   ├── knowledge.rs      # 知识记忆
    │       │   ├── layers.rs         # 记忆层抽象
    │       │   └── storage.rs        # 存储适配
    │       ├── tool/             # 工具适配器、通用工具集封装
    │       │   └── mod.rs
    │       └── vertical/         # 垂直场景能力实现（基于内存存储）
    │           ├── mod.rs
    │           ├── asset.rs      # 内存资产库
    │           ├── project.rs    # 内存项目记忆
    │           ├── tool.rs       # 内存工具注册
    │           └── workflow.rs   # 内存工作流存储
    ├── subhuti/                  # 门面层：面向应用层的统一能力入口（Facade）
    │   ├── Cargo.toml
    │   ├── src/
    │   │   ├── lib.rs            # Facade 入口，统一导出所有能力
    │   │   ├── config.rs         # 配置类型定义
    │   │   ├── debug.rs          # 调试工具
    │   │   └── skill/            # Skill 模块（deprecated）
    │   │       └── mod.rs
    │   └── tests/                # 集成测试
    │       ├── test_llm_mock.rs
    │       ├── graph_actor_demo.rs
    │       └── orchestrator_graph_integration.rs
    ├── subhuti-plugin-sdk/       # 插件 SDK：供第三方开发者扩展专家和工具
    │   ├── Cargo.toml
    │   └── src/
    │       └── lib.rs
    └── subhuti-expert-psychology-wasm/ # 心理学专家 WASM 实现
        ├── Cargo.toml
        └── src/
            └── lib.rs

## 架构分层说明

### 三层架构原则

```
┌─────────────────────────────────────────────────────────┐
│  Application Layer (src/)                              │
│  ├─ 应用层：业务实现，使用 core 接口和 infra 实现      │
│  └─ 包含专家具体业务逻辑、工作流定义、API 路由          │
├─────────────────────────────────────────────────────────┤
│  Facade Layer (crates/subhuti)                         │
│  ├─ 门面层：统一能力入口，屏蔽内部复杂性                │
│  └─ 提供 Subhuti 结构体，聚合所有能力                  │
├─────────────────────────────────────────────────────────┤
│  Infrastructure Layer (crates/subhuti-infra)           │
│  ├─ 基础设施层：具体实现，对接第三方服务                │
│  └─ LLM 客户端、数据库适配、工具集成、记忆实现          │
├─────────────────────────────────────────────────────────┤
│  Core Layer (crates/subhuti-core)                      │
│  ├─ 核心框架层：纯机制，无业务，无第三方 SDK 依赖      │
│  └─ 只定义规则和运行时（接口、事件总线、图引擎、Actor）│
└─────────────────────────────────────────────────────────┘
```

### 设计哲学

**图 + 事件 + Actor 三种设计范式，不是代码分层**：
- **图是骨架**：定义执行流程和数据流转（graph/）
- **事件是血液**：横切所有层的通信管道（event/）
- **Actor 是细胞**：封装状态和行为的执行单元（actor/、graph/actor/）

**框架与应用分离**：
- **框架核心（core）**：空壳，只定义规则和运行时，不含业务细节
- **应用层（src/）**：图、事件、专家的具体业务实现
- **契约**：框架和应用之间靠稳定的抽象接口做契约

### 关键接口

| 接口 | 定义位置 | 实现位置 | 职责 |
|------|---------|---------|------|
| LLM | core/runtime/llm | infra/llm | 大语言模型抽象 |
| Memory | core/memory | infra/memory | 记忆管理抽象 |
| Tool | core/runtime/tools | infra/tool | 工具调用抽象 |
| EventBus | core/event/bus | core/event/bus | 事件总线抽象与实现 |
| Graph | core/graph | core/graph | 图编排抽象与实现 |
| ExpertAgent | core/orchestrator | src/domain/experts | 专家代理抽象与业务实现 |
| GraphOrchestrator | core/orchestrator | src/application | 图编排器抽象与集成实现 |
| AssetLibrary | core/vertical/asset | infra/vertical/asset | 资产库抽象与内存实现 |
| ProjectMemory | core/vertical/project | infra/vertical/project | 项目记忆抽象与内存实现 |
| ToolRegistry | core/vertical/tool | infra/vertical/tool | 工具注册抽象与内存实现 |
| WorkflowStore | core/vertical/workflow | infra/vertical/workflow | 工作流存储抽象与内存实现 |

### 依赖关系

```
subhuti-app (应用层)
    ├── subhuti (门面层)
    │   ├── subhuti-core (核心层)
    │   └── subhuti-infra (基础设施层)
    │       └── subhuti-core (核心层)
    └── subhuti-core (核心层，直接使用接口)

subhuti-plugin-sdk
    └── subhuti-core (核心层，供插件使用)

subhuti-expert-psychology-wasm
    └── subhuti-plugin-sdk
```
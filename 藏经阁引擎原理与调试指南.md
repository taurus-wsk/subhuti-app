# 藏经阁引擎（Sutra Library）原理与调试指南

## 目录

1. [架构总览](#1-架构总览)
2. [核心数据模型](#2-核心数据模型)
3. [写入流水线 —— 如何新增记忆](#3-写入流水线--如何新增记忆)
4. [召回流水线 —— 如何召唤记忆](#4-召回流水线--如何召唤记忆)
5. [实体图谱系统](#5-实体图谱系统)
   - 5.8 [反馈分析器（FeedbackAnalyzer）](#58-反馈分析器feedbackanalyzer)
6. [领域适配器系统](#6-领域适配器系统)
7. [冷热记忆与反馈](#7-冷热记忆与反馈)
   - 7.4 [记忆自进化闭环](#74-记忆自进化闭环)
8. [存储层：内存 + PG 双写](#8-存储层内存--pg-双写)
9. [Blender 专家调用示例](#9-blender-专家调用示例)
10. [调试命令速查表](#10-调试命令速查表)
    - 10.5 [反馈闭环调试](#105-反馈闭环调试)
11. [接入外部 MCP 客户端（WorkBuddy）](#11-接入外部-mcp-客户端workbuddy)
    - 11.5 [三个必踩的坑](#115-三个必踩的坑) / 11.5.1 [能力边界](#1151-接上之后的能力边界)
    - 11.7 [把藏经阁当「纯记忆引擎」用](#117-把藏经阁当纯记忆引擎用只保存--搜索)
12. [召回质量治理（2026-09-14）](#12-召回质量治理2026-09-14)
    - 12.6 [第二轮：入口收敛与测试残留](#126-第二轮入口收敛与测试残留同日稍晚)
    - 12.8 [第四轮：锚点表里的裸「项目」](#128-第四轮锚点表里的裸项目2026-09-14-0240)
    - 12.9 [第六轮：当「纯记忆引擎」验](#129-第六轮当纯记忆引擎验2026-09-14-0250)

---

## 1. 架构总览

### 1.1 设计原则

- **无向量**：完全基于词法检索 + 结构关联 + 图谱关系，不依赖任何向量嵌入
- **物理双层**：内存（毫秒级读写）+ PostgreSQL（唯一持久化）
- **逻辑三层**：临时记忆（会话级）→ 热记忆（高频访问）→ 冷记忆（低频归档）
- **结构三轴**：记忆树（纵向父子）+ 词法检索（横向 BM25）+ 轻量图谱（实体关联）
- **领域插件化**：每个领域实现 `DomainParser` + `DomainTokenizer`，内核永久不变

### 1.2 模块依赖关系

```
SutraLibraryPort (core 层 trait，专家接口)
        ↑
MemoryEnginePort (infra 层引擎，核心入口)
        ├── MemoryStorage (内存存储 + BM25 索引)
        ├── PgStorage (PG 持久化)
        ├── DomainRouter (领域路由)
        │     ├── BlenderDomainParser / BlenderDomainTokenizer
        │     ├── RustDomainParser / RustDomainTokenizer
        │     └── GeneralDomainParser / GeneralDomainTokenizer
        ├── BaseSearch (基础检索)
        ├── SpaceDepthStrategy (空间通路)
        ├── GraphPassageStrategy (图谱通路)
        │     └── EntityGraph (实体图谱)
        ├── QueryAnalyzer (查询分析器)
        ├── RetrievalScheduler (三级检索调度器)
        ├── HotnessCalculator (冷热计算)
        ├── TreeValidator (树结构校验)
        └── FeedbackAnalyzer (反馈分析器)
              └── LlmScorer (LLM 自省打分器)
```

### 1.3 关键文件索引

| 文件 | 说明 |
|------|------|
| `crates/subhuti-core/src/sutra_library.rs` | `SutraLibraryPort` trait 定义（专家接口） |
| `crates/subhuti-infra/src/sutra_library/mod.rs` | 模块入口，`create_sutra_engine()` 构建函数 |
| `crates/subhuti-infra/src/sutra_library/engine.rs` | `MemoryEnginePort` 核心引擎 |
| `crates/subhuti-infra/src/sutra_library/models.rs` | 全部数据模型 |
| `crates/subhuti-infra/src/sutra_library/storage.rs` | `MemoryStorage` + `PgStorage` + `PgGraphStorage` |
| `crates/subhuti-infra/src/sutra_library/recall/mod.rs` | 召回模块入口，定义 `Candidate`/`BaseHit`/`SpaceHit`/`GraphHit` |
| `crates/subhuti-infra/src/sutra_library/recall/pipeline.rs` | 五阶段召回流水线主逻辑 |
| `crates/subhuti-infra/src/sutra_library/recall/config.rs` | `LibraryRetrieveConfig` 配置 |
| `crates/subhuti-infra/src/sutra_library/recall/base_search.rs` | `BaseSearch` 基础检索 |
| `crates/subhuti-infra/src/sutra_library/recall/space.rs` | `SpaceDepthStrategy` 空间通路 |
| `crates/subhuti-infra/src/sutra_library/recall/graph.rs` | `EntityGraph` + `GraphPassageStrategy` 图谱通路 |
| `crates/subhuti-infra/src/sutra_library/recall/scoring.rs` | 分数归一化 |
| `crates/subhuti-infra/src/sutra_library/recall/query.rs` | 查询分析（同义词、别名、意图分类） |
| `crates/subhuti-infra/src/sutra_library/recall/entity_extractor.rs` | 实体提取器 |
| `crates/subhuti-infra/src/sutra_library/domain/mod.rs` | 领域适配器 trait 定义 |
| `crates/subhuti-infra/src/sutra_library/domain/blender.rs` | Blender 领域适配器 |
| `crates/subhuti-infra/src/sutra_library/domain/general.rs` | 通用领域适配器 |
| `crates/subhuti-infra/src/sutra_library/domain/rust.rs` | Rust 领域适配器 |
| `crates/subhuti-infra/src/sutra_library/hotness.rs` | 冷热记忆算法 |
| `crates/subhuti-infra/src/sutra_library/retrieval.rs` | 三级检索调度器 |
| `crates/subhuti-infra/src/sutra_library/skill.rs` | AI 可调用的记忆技能 |
| `crates/subhuti-infra/src/sutra_library/tree.rs` | 树结构校验 |
| `src/domain/experts/blender.rs` | Blender 专家调用示例 |
| `crates/subhuti-infra/src/sutra_library/feedback/mod.rs` | 反馈闭环模块入口 |
| `crates/subhuti-infra/src/sutra_library/feedback/data_models.rs` | 反馈数据模型（ExecutionLog、FeedbackMetrics） |
| `crates/subhuti-infra/src/sutra_library/feedback/analyzer.rs` | FeedbackAnalyzer 核心实现（滑动窗口、共现挖掘、PG 持久化） |
| `crates/subhuti-infra/src/sutra_library/feedback/llm_scorer.rs` | LLM 自省打分器（抽样评估切片相关度） |

---

## 2. 核心数据模型

### 2.1 MemoryNode —— 通用记忆节点

`MemoryNode` 是藏经阁中最核心的数据结构，所有记忆都以节点形式存储。

```rust
// crates/subhuti-infra/src/sutra_library/models.rs:10-46
pub struct MemoryNode {
    // ─── 唯一标识 ───
    pub node_id: String,         // UUID v4，全局唯一
    pub collection_id: String,   // 集合ID（类似数据库表名）
    pub domain: String,          // 领域（"blender", "rust", "general"）
    pub node_type: String,       // 节点类型（如 "blender_script", "document_section"）
    pub content_hash: String,    // SHA256 内容哈希，用于幂等去重

    // ─── 树结构纵向 ───
    pub parent_id: Option<String>, // 父节点 ID（None = 根节点）
    pub path: String,              // 路径（如 "/Blender教程/修改器/阵列修改器"）
    pub depth: u32,                // 树深度（根=0）
    pub sort_order: u32,           // 同级排序序号

    // ─── 内容 ───
    pub title: String,             // 标题
    pub summary: String,           // 摘要（由领域适配器的 enrich_chunk 生成）
    pub content: String,           // 完整内容
    pub metadata: serde_json::Value, // 元数据（JSON）

    // ─── 图谱横向关联 ───
    pub refs_out: Vec<RefEdge>,    // 出边（指向其他节点）
    pub refs_in: Vec<RefEdge>,     // 入边（被其他节点指向）

    // ─── 版本快照 ───
    pub version_tag: String,       // 版本标签（"current" / "snapshot"）
    pub snapshot_id: Option<String>, // 快照 ID

    // ─── 热度生命周期 ───
    pub base_activation: f32,      // 基础激活分 [0, 1]
    pub importance: u8,            // 重要性 [1-10]
    pub access_count: u32,         // 访问次数
    pub feedback_score: f32,       // 反馈分 [-1, 1]
    pub last_accessed_at: i64,     // 最后访问时间戳
    pub created_at: i64,           // 创建时间戳
    pub updated_at: i64,           // 更新时间戳
}
```

### 2.2 Candidate —— 召回候选

```rust
// crates/subhuti-infra/src/sutra_library/recall/mod.rs:76-82
pub struct Candidate {
    pub chunk_id: ChunkUuid,       // 切片 ID（即 MemoryNode.node_id）
    pub base_score: f32,           // 基础得分（来自 BaseSearch 的 BM25）
    pub bonus_score: f32,          // 加分（来自 Space + Graph 通路）
    pub source_flags: Vec<RetrieveSource>, // 来源标记（Base/Space/GraphFirstPass/GraphSecondPass）
}
```

### 2.3 其他关键结构

```rust
// 集合（类似数据库表）
pub struct Collection {
    pub collection_id: String,
    pub name: String,
    pub domain: String,
    pub description: String,
    pub created_at: i64,
}

// 语义切片（领域适配器输出）
pub struct SemanticChunk {
    pub title: String,
    pub content: String,
    pub node_type: String,
    pub parent_path: Option<String>,
    pub sort_order: u32,
    pub metadata: serde_json::Value,
}

// 检索请求
pub struct RetrievalQuery {
    pub text: String,
    pub collection_id: Option<String>,
    pub domain: Option<String>,
    pub node_type: Option<String>,
    pub limit: usize,
    pub mode: ContextMode,  // Precise / Standard / Deep
}
```

---

## 3. 写入流水线 —— 如何新增记忆

### 3.1 入口函数

**`SutraLibraryPort::write()`** — 专家调用的顶层接口

```rust
// crates/subhuti-core/src/sutra_library.rs:26
fn write(&self, collection_id: &str, content: &str, domain: &str, parent_id: Option<&str>) -> String;
```

**`MemoryEnginePort::write_node()`** — 核心写入方法

```rust
// crates/subhuti-infra/src/sutra_library/engine.rs:129-272
pub fn write_node(
    &self,
    collection_id: &str,
    raw_content: &str,
    domain: &str,
    parent_id: Option<&str>,
) -> Result<MemoryNode>;
```

### 3.2 写入流水线步骤

```
write_node() 调用链路：
  1. 输入校验         → 检查 raw_content 是否为空
  2. 领域路由         → domain_router.parser_for(domain) 获取领域解析器
  3. 幂等去重前置     → SHA256 内容哈希，检查是否已存在
  4. 领域语义切片     → parser.split_semantic_chunks() 将原始内容切成语义块
  5. 摘要元数据增强   → parser.enrich_chunk() 为每个切片生成摘要和元数据
  6. 树结构挂载+校验  → TreeValidator 计算路径/深度，检查环和跨集合
  7. 内存写入         → memory.write_node() 写入内存存储
  8. 图谱关联提取     → parser.extract_edges() 提取节点间 RefEdge
  9. 实体图谱注册     → entity_graph.register_chunk() 提取实体并建立索引
  10. 领域实体关系    → parser.extract_entity_relations() → EntityGraph Manual 边
  11. 异步 PG 落库    → 后台 spawn 写入 PostgreSQL
```

### 3.3 如何新增一条记忆 —— 调试步骤

**通过 curl 直接写入记忆**（需要先有集合）：

```bash
# 1. 先查看已有集合
curl -s http://localhost:8615/subhuti/api/v1/sutra/collections

# 2. 如果没有 blender_knowledge 集合，先创建（通过专家调用自动创建）
# 或者直接发送编排请求让专家自动创建
curl -s -X POST http://localhost:8615/subhuti/api/v1/orchestrate \
  -H "Content-Type: application/json" \
  -d '{"message":"Blender 阵列修改器可以将物体沿指定轴重复排列，非常适合循环建模。请记住这个知识。","graph":"default"}'

# 3. 查看写入后的统计
curl -s http://localhost:8615/subhuti/api/v1/sutra/stats

# 4. 查看所有节点
curl -s http://localhost:8615/subhuti/api/v1/sutra/nodes
```

> **注意**：目前 `/subhuti/api/v1/sutra/*` 路由是否存在取决于是否注册了 sutra 相关的 HTTP 路由。如果不存在，可以通过 Python 脚本或直接通过 `curl` 调用编排接口间接写入。

### 3.4 写入流水线的关键函数说明

| 函数 | 所在文件 | 作用 |
|------|----------|------|
| `write_node()` | `engine.rs:129` | 写入入口，执行完整写入流水线 |
| `hash_content()` | `engine.rs:508` | SHA256 内容哈希，用于幂等去重 |
| `parser_for()` | `domain/mod.rs:59` | 按领域名获取解析器 |
| `split_semantic_chunks()` | `domain/blender.rs:182` | 领域语义切片（Blender 版解析脚本元素） |
| `enrich_chunk()` | `domain/blender.rs:210` | 生成摘要和元数据 |
| `compute_path()` | `tree.rs:83` | 计算节点路径 |
| `validate_mount()` | `tree.rs:55` | 树结构合法性校验（防环、防跨集合） |
| `register_chunk()` | `recall/graph.rs:129` | 注册切片到实体图谱，提取实体并建立索引 |
| `extract_entity_relations()` | `domain/blender.rs:348` | 提取领域实体关系对 → Manual 边 |
| `add_manual_edge()` | `recall/graph.rs:162` | 向图谱添加手动关系边 |

---

## 4. 召回流水线 —— 如何召唤记忆

### 4.1 入口函数

**`SutraLibraryPort::library_retrieve()`** — 专家调用的顶层接口

```rust
// crates/subhuti-core/src/sutra_library.rs:38
async fn library_retrieve(&self, query: &str, top_k: usize) -> String;
```

**`MemoryEnginePort::library_retrieve()`** — 核心召回方法

```rust
// crates/subhuti-infra/src/sutra_library/engine.rs:349-366
pub async fn library_retrieve(
    &self,
    query: &str,
    config: &LibraryRetrieveConfig,
) -> Vec<recall::Candidate>;
```

### 4.2 五阶段召回流水线

```
library_retrieve() 调用链路：
  ┌─────────────────────────────────────────────────────────────┐
  │ 阶段0: 查询分析                                              │
  │ query_analyzer.analyze(query)                                │
  │   → 意图分类 (Precise/Standard/Deep)                         │
  │   → 同义词扩展 (如 "pg" → "PostgreSQL 数据库")               │
  │   → 别名展开 (如 "bm25" → "BM25 检索算法")                   │
  │   → 关键词提取 (中文 N-gram + 英文分词)                      │
  └─────────────────────────────────────────────────────────────┘
                              │
  ┌─────────────────────────────────────────────────────────────┐
  │ 阶段1: 基础检索 BaseSearch                                   │
  │ base_search.search(expanded_queries, top_k)                  │
  │   → BM25 检索（主要）：词频 + 逆文档频率                     │
  │   → 关键词补充（未命中 BM25 的节点给基础分 0.1）             │
  │   → 多扩展查询合并，取最大 base_score                        │
  │   → 输出: Seed0（原始种子切片列表）                          │
  └─────────────────────────────────────────────────────────────┘
                              │
  ┌─────────────────────────────────────────────────────────────┐
  │ 阶段2: 并行一阶扩展通路                                      │
  │                                                              │
  │ 通路A: SpaceDepthStrategy（静态空间）                         │
  │   space_strategy.expand(seed0, root_entity_set, max_dist,    │
  │                          per_level_limit)                     │
  │   → 树距离1: 同 collection（同柜子），bonus=0.15             │
  │   → 树距离2: 同 domain（同阁），bonus=0.05                   │
  │   → 排序: 按实体加权重合度降序（反馈加权）                   │
  │                                                              │
  │ 通路B: GraphPassageStrategy 一阶图谱 BFS                     │
  │   graph_strategy.expand(seed0, graph_cfg, score_scale=1.0)  │
  │   → 从种子实体出发 BFS 遍历 1 跳                             │
  │   → Manual 边全权重，Learned 边 × learned_scale              │
  │   → 跳数衰减 × 全局倍率                                      │
  └─────────────────────────────────────────────────────────────┘
                              │
  ┌─────────────────────────────────────────────────────────────┐
  │ 阶段3: 可选二阶图谱扩展（配置开关）                           │
  │   graph_strategy.expand(space_chunk_seeds, graph_cfg,        │
  │                        second_pass_score_decay)              │
  │   → 以空间通路的命中切片为种子，再投喂图谱 BFS               │
  │   → 分数倍率 = second_pass_score_decay（默认0.65）           │
  └─────────────────────────────────────────────────────────────┘
                              │
  ┌─────────────────────────────────────────────────────────────┐
  │ 阶段4: 合并候选池                                            │
  │   → Base / Space / GraphFirstPass / GraphSecondPass          │
  │   → 相同 chunk_id 合并，取最大 bonus_score                   │
  │   → 记录来源标记 (source_flags)                              │
  └─────────────────────────────────────────────────────────────┘
                              │
  ┌─────────────────────────────────────────────────────────────┐
  │ 阶段5: 归一化 + 排序                                         │
  │   → rank-based 归一化：每个通路内部按排名归一化到 [0,1]      │
  │   → 总分 = base_score（归一化后）+ bonus_score（归一化后）   │
  │   → 按总分降序排序                                           │
  └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
                     返回 Vec<Candidate>
```

### 4.3 召回配置详解

```rust
// crates/subhuti-infra/src/sutra_library/recall/config.rs:5-26
pub struct LibraryRetrieveConfig {
    // 基础检索
    pub base_top_k: usize,              // 基础检索取 top K（默认8）

    // 空间通路
    pub space_enable: bool,              // 是否启用空间通路（默认true）
    pub space_max_tree_distance: u32,    // 最大树距离（0=同抽屉, 1=同柜子, 2=同阁）
    pub space_per_level_limit: usize,    // 每层截断数（默认8）

    // 图谱配置
    pub graph_max_depth: usize,          // BFS 最大深度（默认2）
    pub graph_max_global_result: usize,  // 全局结果截断数（默认12）
    pub graph_decay_factor: f32,         // 跳数衰减因子（默认0.7）
    pub enable_manual_edge: bool,        // 是否启用 Manual 边（默认true）
    pub enable_learned_edge: bool,       // 是否启用 Learned 边（默认true）
    pub learned_edge_scale: f32,         // Learned 边权重倍率（默认0.4）

    // 二阶图谱
    pub graph_second_pass_from_space: bool, // 空间切片投喂二阶图谱（默认false）
    pub second_pass_score_decay: f32,       // 二阶图谱分数衰减（默认0.65）
    pub second_pass_max_result: usize,      // 二阶图谱结果截断（默认8）
}
```

**预设配置模式**：

| 模式 | 方法 | 说明 |
|------|------|------|
| 标准 | `Default::default()` | 日常检索，基础+空间+图谱一阶 |
| 深度问答 | `LibraryRetrieveConfig::deep_qa()` | 打开二阶图谱，适合复杂问题 |
| 精准检索 | `LibraryRetrieveConfig::precise()` | 仅同抽屉，无图谱 |
| 模块宽松 | `LibraryRetrieveConfig::module_relaxed()` | 同柜子，无二阶图谱 |

### 4.4 如何调试召回过程

**通过 curl 触发召回**（通过专家调用）：

```bash
# 发送一个查询，让 Blender 专家执行 library_retrieve
curl -s -X POST http://localhost:8615/subhuti/api/v1/orchestrate \
  -H "Content-Type: application/json" \
  -d '{"message":"阵列修改器怎么用","graph":"default"}' | python3 -m json.tool | head -30
```

**查看服务日志中的召回详情**：

```bash
# 实时追踪日志（需要先修复日志级别问题）
tail -f logs/subhuti.log | grep -E "Blender 新版|候选池|归一化|检索到"

# 查看最终排序后的候选（JSON 格式日志）
grep "候选池" logs/subhuti.log.2026-08-16 | tail -5
```

### 4.5 召回流水线的关键函数说明

| 函数 | 所在文件 | 作用 |
|------|----------|------|
| `library_retrieve()` (pipeline) | `recall/pipeline.rs:42` | 五阶段召回主入口 |
| `default_query_analyzer()` | `recall/pipeline.rs:25` | 全局默认查询分析器 |
| `analyze()` | `recall/query.rs:136` | 查询分析（意图分类、扩展、关键词） |
| `expand_query()` | `recall/query.rs:248` | 同义词扩展 + 别名展开 |
| `classify()` | `recall/query.rs:151` | 查询意图分类 |
| `extract_keywords()` | `recall/query.rs:195` | 中文 N-gram + 英文关键词提取 |
| `BaseSearch::search()` | `recall/base_search.rs:22` | BM25 + 关键词检索 |
| `SpaceDepthStrategy::expand()` | `recall/space.rs:45` | 静态空间扩散 |
| `sort_by_entity_overlap()` | `recall/space.rs:132` | 按实体加权重合度排序 |
| `entity_weight()` | `recall/space.rs:160` | 获取实体反馈加权权重 |
| `GraphPassageStrategy::expand()` | `recall/graph.rs:481` | 图谱 BFS 扩展 |
| `get_neighbors()` | `recall/graph.rs:247` | 获取实体邻居（按边类型过滤） |
| `normalize_candidates()` | `recall/scoring.rs:27` | 按通路 rank-based 归一化 |
| `final_score()` | `recall/scoring.rs:72` | 计算最终总分 |

---

## 5. 实体图谱系统

### 5.1 边类型

| 边类型 | 来源 | 说明 | 衰减 |
|--------|------|------|------|
| `Manual` | 领域解析器 `extract_entity_relations()` | 人工/LLM 定义的关系 | 不衰减 |
| `Learned` | 后台 `observe_entity_cooccurrence()` | 共现熟练度 | 每 24h ×0.9 |

### 5.2 核心数据结构

```rust
// crates/subhuti-infra/src/sutra_library/recall/graph.rs
pub struct EntityGraph {
    graph: RwLock<UnGraph<EntityUuid, EdgeWeight>>,        // petgraph 无向图
    node_indices: RwLock<HashMap<EntityUuid, NodeIndex>>,  // 实体 → 节点索引
    entity_to_chunks: RwLock<HashMap<EntityUuid, Vec<ChunkUuid>>>,
    chunk_to_entities: RwLock<HashMap<ChunkUuid, Vec<EntityUuid>>>,
    entity_feedback: RwLock<HashMap<EntityUuid, f32>>,
    graph_store: RwLock<Option<Arc<dyn GraphPersistence>>>, // PG / SQLite 双写后端
}
```

> **图谱持久化后端（`GraphPersistence` 窄接口）**
>
> `graph_store` 依赖的是图谱专用窄接口而非具体 PG 类型，因此
> **PG 与 SQLite 两种后端都能接入**，无后端时退化为纯内存：
> - `PgGraphStorage` → PG 模式
> - `SqliteStorage` → 无 PG 时的降级模式（运行时双写同样落盘）
>
> 注入点：`create_sutra_engine` 后台初始化时按后端可用性调用
> `MemoryEnginePort::init_graph_persistence(...)`。
> 此前 SQLite 模式下 `graph_store` 恒为 `None`，双写分支永不进入，
> 图谱三张表只有启动回灌、没有运行时增量（`graph_entity_chunks` 恒 0）。

> **数据正确性约束（2026-09-14 补）**：无向图落盘有三处易踩的坑，
> 已在存储层与回灌侧统一收口。任何新增写边路径都必须遵守：
>
> | 约束 | 收口位置 | 不遵守的后果 |
> |------|----------|--------------|
> | **端点归一化**：写/删前按 `(min,max)` 排序 | `persistence.rs::normalize_edge_endpoints`（PG 与 SQLite 双实现都调用） | 同一对实体以 `(A,B)`、`(B,A)` 各存一行；回灌双向展开使边数虚高，`delete_edge` 也只删得掉一半，重启"复活" |
> | **kind 取值统一**：只允许 `Manual` / `Learned` | 各 `write_edge` 调用点 | 反序列化的 fallback 分支把未知值一律当 `Learned`，小写 `manual` 会被误判成可衰减的学习边 |
> | **回灌按 `(min,max)` 去重** | `recall/graph.rs::hashmap_to_graph` | petgraph 的 `add_edge(A,B)` 与 `add_edge(B,A)` 是**两条平行边**（不像 `find_edge` 会视为同一条），内存边数成倍虚高（实测 DB 168 行 → 内存 672 条，4 倍） |
>
> 历史脏数据已用 `scripts/debug/dedup_graph_edges.py` 收敛过一次
> （348 行 → 168 行，清零 56 行小写 kind 与 124 对方向重复）。
> 该脚本默认 dry-run，加 `--apply` 才写库并自动备份。

> **后端验证状态（2026-09-14）**：当前只承诺并验证 **SQLite 路径**。
>
> | 后端 | 状态 | 说明 |
> |------|------|------|
> | `SqliteStorage` | ✅ **已验证** | 运行时双写 + 回灌 + 边数收敛，端到端实测通过（`graph_entity_chunks` 0→98、`edges` 与 DB 逐位对齐）；3 个回归测试锁定 |
> | `PgGraphStorage` | ⏸ **接口就位、未真机验证** | 本机无 PG 环境。实现为对固有方法的**真转发**（非占位桩），6 个方法签名与 trait 一致，且 `write_edge`/`delete_edge` **同样调用了** `normalize_edge_endpoints`，因此数据正确性修复对 PG 一并生效 |
>
> **后续接 PG 时的验证清单**（按序执行即可）：
> 1. 启动日志确认 `init_graph_persistence` 走 PG 分支（而非 SQLite 分支）
> 2. `graph_edges` / `graph_entity_chunks` 写入后行数 > 0，且无 `(A,B)`+`(B,A)` 方向重复
> 3. 观察 `edge_kind` 只出现 `Manual` / `Learned`（无小写残留）
> 4. 重启一次，`/sutra/stats` 的 `edges` / `entity_chunks` 与 PG 直查一致（不虚高）
> 5. 触发一次 like/dislike，确认 `graph_entity_feedback` 有行落盘

### 5.3 实体提取流程

```
write_node() 时自动触发:
  MemoryNode
    → extract_entities_from_node(node)   [entity_extractor.rs]
      → 从 node_id 提取（自身 ID 作为实体）
      → 从 title 提取（中文 N-gram + 英文分词）
      → 从 summary 提取
      → 从 content 提取（前 500 字符）
      → 从 domain 提取（"domain:blender"）
      → 从 metadata.tags / metadata.keywords 提取
    → EntityGraph.register_chunk(node)
      → 更新 chunk_to_entities 索引
      → 更新 entity_to_chunks 索引
      → 同 chunk 内实体两两建共现边（link_cooccurrence）
      → 异步双写（PG / SQLite，写 graph_entity_chunks + 共现边）
```

### 5.4 实体关系提取流程

```
write_node() 时自动触发:
  MemoryNode
    → parser.extract_entity_relations(&node.content)  [domain/blender.rs:348]
      → 遍历内置知识库关系对
      → 检查内容中是否同时包含实体A和实体B
      → 如果是，返回 (entityA, entityB, weight)
    → EntityGraph.add_manual_edge(entityA, entityB, weight)
      → 写入 petgraph 无向图（Manual 边）
      → 异步双写（PG / SQLite，写 graph_edges）
```

### 5.5 共现学习（后台任务）

```rust
// crates/subhuti-infra/src/sutra_library/engine.rs:372-383
pub fn post_retrieve_learn(&self, candidates: &[recall::Candidate]) {
    // 1. 从候选结果中提取 chunk_ids
    // 2. 从图谱索引获取 chunk 关联的实体集合
    // 3. 实体两两共现，增量更新 Learned 边
    // 4. 异步双写（PG / SQLite）
}
```

### 5.6 反馈反哺

```rust
// crates/subhuti-infra/src/sutra_library/engine.rs:478-498
pub fn apply_feedback(&self, node_id: &str, positive: bool) {
    // 1. 更新 node.feedback_score ±0.1
    // 2. 异步双写（PG / SQLite，写 graph_entity_feedback）
    // 3. 反哺实体图谱：
    //    - entity_feedback 累计分 ±0.15
    //    - Learned 边权重 ±delta×0.5
    //    - Manual 边权重 ±delta×0.1（微弱影响）
}
```

### 5.7 图谱关键函数说明

> 注：下表行号可能随迭代漂移，以当前代码为准。

| 函数 | 所在文件 | 作用 |
|------|----------|------|
| `register_chunk()` | `recall/graph.rs` | 注册切片到图谱，提取实体索引 |
| `add_manual_edge()` | `recall/graph.rs` | 添加手动边（异步双写 PG / SQLite） |
| `add_learned_edge()` | `recall/graph.rs` | 添加学习边（跳过 Manual 已存在的边） |
| `observe_entity_cooccurrence()` | `recall/graph.rs` | 观察实体共现，增量更新 Learned 边 |
| `decay_learned_edges()` | `recall/graph.rs` | 衰减 Learned 边权重（<0.05 删除） |
| `apply_feedback_to_entities()` | `recall/graph.rs` | 反馈反哺图谱边权重 |
| `get_entity_weighted_score()` | `recall/graph.rs` | 获取实体反馈加权权重（用于空间排序） |
| `get_entities_of_chunks()` | `recall/graph.rs` | 获取切片关联的实体集合 |
| `get_chunks_of_entities()` | `recall/graph.rs` | 获取实体关联的切片 |
| `get_neighbors()` | `recall/graph.rs` | 获取实体邻居（按边类型过滤） |
| `load_persisted_data()` | `recall/graph.rs` | 设置持久化后端并异步回灌图谱（PG / SQLite） |
| `GraphPersistence` | `recall/graph.rs` | 图谱持久化窄接口（PG / SQLite 双实现） |
| `start_decay_task()` | `engine.rs` | 启动 Learned 边定时衰减任务 |
| `record_execution()` | `engine.rs` | 记录执行日志到反馈分析器 |
| `FeedbackAnalyzer::record_execution()` | `feedback/analyzer.rs` | 滑动窗口记录 + 轻量实时共现分析 |
| `FeedbackAnalyzer::analyze_cooccurrence()` | `feedback/analyzer.rs:242` | 实体共现挖掘核心逻辑 |
| `FeedbackAnalyzer::get_metrics()` | `feedback/analyzer.rs:104` | 计算当前命中率指标 |
| `FeedbackAnalyzer::init_pg_tables()` | `feedback/analyzer.rs:325` | 初始化 PG 表（逐条 SQL） |

### 5.8 反馈分析器（FeedbackAnalyzer）

FeedbackAnalyzer 是藏经阁的记忆自进化闭环核心组件，负责从专家执行日志中自动挖掘实体关联并更新 Learned 边。

#### 数据模型

```rust
// feedback/data_models.rs:12-32
pub struct ExecutionLog {
    pub query_hash: String,              // Query 的 Sha256 哈希（去重/分组）
    pub query: String,                   // 原始 Query 文本
    pub recalled_chunks: Vec<RecallChunkInfo>, // 召回的全部切片信息
    pub used_chunk_ids: Vec<String>,     // Agent 实际使用的切片 ID 列表
    pub task_success: bool,              // 任务执行是否成功
    pub timestamp: i64,                  // 时间戳（Unix 毫秒）
    pub graph: String,                   // 对话图谱 ID
    pub domain: String,                  // 领域
    pub session_id: Option<String>,      // 会话 ID（可选）
}

pub struct FeedbackMetrics {
    pub total_queries: usize,            // 总查询次数
    pub hit_rate: f64,                   // 命中率（used_chunks / recalled_chunks）
    pub source_contribution: HashMap<String, f64>, // 各通路贡献占比
    pub avg_used_chunks: f64,           // 平均实际使用切片数
    pub success_rate: f64,              // 成功任务占比
    // ...
}
```

#### 双模式运行

1. **实时轻量模式**：每条执行日志进来，放进内存滑动窗口，只做小幅度熟练度更新
2. **定时批量模式（推荐）**：每隔 5 分钟，批量拉取日志，用滑动窗口快照做完整共现分析，批量更新图谱

#### 实体共现挖掘逻辑

```
record_execution(ExecutionLog)
  → 从 used_chunk_ids 获取切片集合
  → 提取每个切片关联的实体（entity_graph.get_entities_of_chunks）
  → 集合内实体两两配对 (E1, E2)
  → 任务成功 → 熟练度 +0.15；任务失败 → 熟练度 +0.075
  → 更新 EntityGraph Learned 边（add_learned_edge）
  → 异步 PG 持久化
```

#### 后台分析任务

```
start_background_analysis() (每 5 分钟)
  → 从滑动窗口获取日志快照
  → 执行完整共现分析（analyze_cooccurrence）
  → 计算命中率指标（get_metrics）
  → 持久化反馈指标到 PG（sutra_feedback_metrics 表）
  → 输出日志：命中率、成功率
```

#### PG 表结构

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `sutra_execution_logs` | 执行日志持久化 | query_hash, query, recalled_chunks(JSONB), used_chunk_ids(TEXT[]), task_success, domain |
| `sutra_feedback_metrics` | 反馈指标快照 | graph, domain, total_queries, hit_rate, source_contribution(JSONB), success_rate |

---

## 6. 领域适配器系统

### 6.1 DomainParser Trait

```rust
// crates/subhuti-infra/src/sutra_library/domain/mod.rs:13-31
pub trait DomainParser: Send + Sync {
    fn domain_name(&self) -> &str;
    fn split_semantic_chunks(&self, raw: &str, ctx: &ParseContext) -> Vec<SemanticChunk>;
    fn enrich_chunk(&self, chunk: &SemanticChunk) -> (String, serde_json::Value);
    fn extract_edges(&self, nodes: &[MemoryNode]) -> Vec<RefEdge>;
    fn incremental_parse(&self, old: &MemoryNode, diff: &str) -> Result<MemoryNode>;
    fn format_context(&self, node: &MemoryNode, mode: ContextMode) -> String;
    fn compress_nodes(&self, nodes: &[MemoryNode]) -> MemoryNode;
    fn rerank_nodes(&self, query: &str, candidates: &mut Vec<ScoredNode>);

    // 可选：提取实体关系对 → EntityGraph Manual 边
    fn extract_entity_relations(&self, _content: &str) -> Vec<(String, String, f32)> {
        Vec::new()  // 默认空实现
    }
}
```

### 6.2 DomainTokenizer Trait

```rust
// crates/subhuti-infra/src/sutra_library/domain/mod.rs:34-38
pub trait DomainTokenizer: Send + Sync {
    fn tokenize(&self, text: &str) -> Vec<String>;
    fn stop_words(&self) -> &[&str];
    fn expand_query(&self, query: &str) -> Vec<String>;
}
```

### 6.3 Blender 领域适配器详解

**BlenderDomainParser 的关键方法**：

| 方法 | 行号 | 作用 |
|------|------|------|
| `extract_blender_items()` | blender.rs:23 | 从 Python 脚本中提取场景/对象/材质/修改器/动画 |
| `blender_entity_relations()` | blender.rs:108 | 内置 33 条 Blender 知识关系对 |
| `extract_blender_entity_pairs()` | blender.rs:147 | 从内容中匹配知识库关系对 |
| `split_semantic_chunks()` | blender.rs:182 | 按提取的元素切片，无元素则整体作为脚本节点 |
| `enrich_chunk()` | blender.rs:210 | 生成摘要 `[Blender xxx] 标题` |
| `extract_edges()` | blender.rs:223 | 提取节点间 RefEdge（材质↔对象、修改器↔对象） |
| `extract_entity_relations()` | blender.rs:348 | 委托给 `extract_blender_entity_pairs()` |

**BlenderDomainTokenizer 的关键方法**：

| 方法 | 行号 | 作用 |
|------|------|------|
| `tokenize()` | blender.rs:363 | 分词 + 下划线拆分 |
| `stop_words()` | blender.rs:386 | 46 个 Blender 相关停用词 |
| `expand_query()` | blender.rs:394 | 蛇形/空格互转 + 术语映射 |

### 6.4 注册新领域

```rust
// crates/subhuti-infra/src/sutra_library/mod.rs:49-61
router.register(
    Box::new(NewDomainParser::new()),
    Box::new(NewDomainTokenizer::new()),
);
```

---

## 7. 冷热记忆与反馈

### 7.1 热度计算

```rust
// crates/subhuti-infra/src/sutra_library/hotness.rs:17-26
pub fn compute_activation(node: &MemoryNode, now_ts: i64) -> f32 {
    let base = node.base_activation;        // 基础激活分
    let time_decay = time_decay();           // 指数衰减，半衰期 7 天
    let freq_bonus = frequency_bonus();      // 对数增长，避免过热
    let importance_bonus = importance_bonus();// 重要性 0.5/1.0/1.5/2.0
    let feedback_bonus = feedback_bonus();   // 反馈 [-1,1] → [0.5,1.5]

    activation = base × time_decay × freq_bonus × importance_bonus × feedback_bonus
    clamp(0.0, 1.0)
}
```

**判断标准**：
- `is_hot()`: activation > 0.6
- `is_cold()`: activation < 0.2

### 7.2 三级检索

```rust
// crates/subhuti-infra/src/sutra_library/retrieval.rs:32-100
pub async fn search(&self, query: &RetrievalQuery) -> RetrievalResult {
    // 1. 临时会话记忆（搜索所有 session）
    // 2. 热内存检索（BM25 + 关键词，热节点加分）
    // 3. PG 冷库（仅返回冷节点，避免重复）
}
```

### 7.3 反馈机制

```rust
// 正反馈（like）
pub fn like(&self, node_id: &str) -> String;    // feedback_score += 0.1

// 负反馈（dislike）
pub fn dislike(&self, node_id: &str) -> String;  // feedback_score -= 0.1

// 沉淀会话到持久记忆
pub fn precipitate(&self, session_id: &str, collection_id: &str, domain: &str) -> String;

// 记录执行日志到反馈分析器（反馈闭环入口）
pub fn record_execution(
    &self,
    query: &str,
    task_success: bool,
    graph: &str,
    domain: &str,
    session_id: Option<String>,
) -> String;
```

### 7.4 记忆自进化闭环

FeedbackAnalyzer 在启动时自动初始化 PG 表，后台启动定时分析任务：

```
create_sutra_engine() [mod.rs:37]
  → MemoryEnginePort::new()
    → FeedbackAnalyzer::new()           [创建分析器]
  → 如果 PG 已配置：
    → feedback_analyzer.init_pg_tables()  [建表（幂等）]
    → feedback_analyzer.start_background_analysis()  [启动后台分析]
```

**关键函数说明**：

| 函数 | 所在文件 | 作用 |
|------|----------|------|
| `record_execution()` | `engine.rs:128` | 记录执行日志（MemoryEnginePort 方法，专家专用） |
| `record_execution()` (trait) | `engine.rs:834` | SutraLibraryPort trait 实现，自动缓存检索结果 |
| `FeedbackAnalyzer::record_execution()` | `feedback/analyzer.rs:78` | 滑动窗口记录 + 轻量实时共现分析 |
| `FeedbackAnalyzer::get_metrics()` | `feedback/analyzer.rs:104` | 计算当前命中率指标 |
| `FeedbackAnalyzer::analyze_cooccurrence()` | `feedback/analyzer.rs:242` | 实体共现挖掘核心逻辑 |
| `FeedbackAnalyzer::start_background_analysis()` | `feedback/analyzer.rs:188` | 启动后台定时分析任务 |
| `FeedbackAnalyzer::init_pg_tables()` | `feedback/analyzer.rs:325` | 初始化 PG 表（逐条执行 SQL） |
| `LlmScorer::should_sample()` | `feedback/llm_scorer.rs:40` | 判断是否应进行 LLM 自省采样 |

---

## 8. 存储层：内存 + PG 双写

### 8.1 内存存储 MemoryStorage

- 所有节点存储在 `HashMap<String, MemoryNode>`（RwLock）
- BM25 索引：简易实现，支持 `bm25_search()` 和 `keyword_search()`
- 会话记忆：`HashMap<String, Vec<MemoryNode>>` 按 session_id 隔离

### 8.2 PostgreSQL 持久化

- **PgStorage**: 节点表 `memory_nodes`、集合表 `memory_collections`、边表 `memory_edges`、快照表 `memory_snapshots`
- **PgGraphStorage**: 图谱边表 `graph_edges`、实体-块映射表 `graph_entity_chunks`、实体反馈表 `graph_entity_feedback`
- **FeedbackAnalyzer 额外表**: 执行日志表 `sutra_execution_logs`、反馈指标表 `sutra_feedback_metrics`
- 启动时自动建表（`ensure_tables()` + `init_pg_tables()`），已存在则跳过
- 写入采用异步双写（内存先写，后台 spawn 写 PG）
- PG 连接失败自动降级为纯内存模式（warn 日志）

### 8.3 启动加载流程

```
create_sutra_engine() [mod.rs:37]
  → MemoryStorage::new() (空内存)
  → DomainRouter::register() (注册领域适配器)
  → MemoryEnginePort::new() (构建引擎)
  → 如果 PG 已配置：
    → pg.ensure_tables() (建表)
    → PgGraphStorage::ensure_tables() (建图谱表)
    → engine.init_graph_pg_async() (后台加载 PG 数据到图谱)
```

---

## 9. Blender 专家调用示例

### 9.1 当前调用流程

```rust
// src/domain/experts/blender.rs:99-200
async fn run(&self, exec_ctx: DomainExecutionContext) -> DomainResult<String> {
    // 1. 检查并创建 blender_knowledge 集合
    // 2. add_session() 保存当前输入到会话临时记忆
    // 3. library_retrieve() 执行五阶段召回
    // 4. 将检索结果注入 system prompt
    // 5. 调用 LLM.chat() 获取回答
    // 6. record_execution() 记录执行日志到反馈分析器（反馈闭环入口）
    // 7. 保存专家结果到仓库
}
```

### 9.2 完整调试流程

**步骤 1: 启动服务**

```bash
cd /Users/hezenghui/RustroverProjects/subhuti-app
cargo run -- serve --debug --log-level trace
```

**步骤 2: 写入知识（让专家自动写入）**

```bash
# 发送 Blender 知识内容，触发 write_node 写入藏经阁
curl -s -X POST http://localhost:8615/subhuti/api/v1/orchestrate \
  -H "Content-Type: application/json" \
  -d '{"message":"阵列修改器 Array Modifier 可以通过 Count 参数控制重复数量，Offset 控制间距，支持恒定偏移和相对偏移。循环建模中常用阵列修改器配合细分修改器创建复杂几何体。","graph":"default"}'
```

**步骤 3: 验证写入**

```bash
# 查看统计
curl -s http://localhost:8615/subhuti/api/v1/sutra/stats

# 查看图谱状态（PG 直查）
psql -h localhost -p 5432 -U subhuti -d subhuti -c "SELECT COUNT(*) FROM graph_edges;"
psql -h localhost -p 5432 -U subhuti -d subhuti -c "SELECT COUNT(*) FROM graph_entity_chunks;"
```

**步骤 4: 召回验证**

```bash
# 发送查询，触发 library_retrieve
curl -s -X POST http://localhost:8615/subhuti/api/v1/orchestrate \
  -H "Content-Type: application/json" \
  -d '{"message":"阵列修改器怎么用","graph":"default"}' | python3 -c "
import sys, json
d = json.load(sys.stdin)
print('专家:', d['data']['expert_chain'])
print('回答:', d['data']['output'][:300])
"
```

**步骤 5: 查看日志**

```bash
# 查看召回流水线输出
grep -E "Blender 新版|候选池|归一化|检索到|Seed0|空间通路|图谱" logs/subhuti.log.2026-08-16 | tail -20
```

### 9.3 手动验证 EntityGraph 数据

```bash
# 查看 PG 中的图谱边
psql -h localhost -p 5432 -U subhuti -d subhuti << 'SQL'
SELECT from_entity, to_entity, edge_kind, weight
FROM graph_edges
ORDER BY weight DESC
LIMIT 20;
SQL

# 查看实体-块映射
psql -h localhost -p 5432 -U subhuti -d subhuti << 'SQL'
SELECT entity, chunk_id
FROM graph_entity_chunks
LIMIT 20;
SQL

# 查看实体反馈
psql -h localhost -p 5432 -U subhuti -d subhuti << 'SQL'
SELECT * FROM graph_entity_feedback LIMIT 20;
SQL
```

---

## 10. 调试命令速查表

### 10.1 服务管理

| 命令 | 说明 |
|------|------|
| `cargo run -- serve --debug --log-level trace` | 启动服务（调试模式） |
| `cargo run -- serve --mock --debug` | 启动服务（Mock LLM 模式） |
| `cargo run -- serve --mock-file config/subhuti.md` | 启动服务（自定义 Mock 文件） |
| `cargo test --test sutra_library_live -- --test-threads=1` | 运行藏经阁集成测试 |

### 10.2 数据查看

| 命令 | 说明 |
|------|------|
| `curl http://localhost:8615/subhuti/api/v1/sutra/stats` | 查看统计 |
| `curl http://localhost:8615/subhuti/api/v1/sutra/collections` | 查看集合列表 |
| `curl http://localhost:8615/subhuti/api/v1/sutra/nodes` | 查看所有节点 |
| `curl http://localhost:8615/subhuti/api/v1/sutra/nodes/{id}` | 查看单个节点 |

### 10.3 召回测试

| 命令 | 说明 |
|------|------|
| `curl -X POST -d '{"message":"...","graph":"default"}' http://localhost:8615/subhuti/api/v1/orchestrate` | 发送编排请求触发召回 |
| `grep "候选池" logs/subhuti.log* \| tail -10` | 查看召回候选池 |
| `grep "Blender 新版" logs/subhuti.log* \| tail -5` | 查看 Blender 召回日志 |

### 10.4 PG 直查

| 命令 | 说明 |
|------|------|
| `psql -h localhost -U subhuti -d subhuti -c "SELECT COUNT(*) FROM memory_nodes;"` | 节点总数 |
| `psql -h localhost -U subhuti -d subhuti -c "SELECT COUNT(*) FROM graph_edges;"` | 图谱边数 |
| `psql -h localhost -U subhuti -d subhuti -c "SELECT COUNT(*) FROM graph_entity_chunks;"` | 实体-块映射数 |
| `psql -h localhost -U subhuti -d subhuti -c "SELECT * FROM graph_edges ORDER BY weight DESC LIMIT 10;"` | 图谱边 Top 10 |

### 10.5 反馈闭环调试

| 命令 | 说明 |
|------|------|
| `docker exec pgvector-db-new psql -U postgres -d postgres -c "SELECT * FROM sutra_execution_logs ORDER BY id;"` | 查看执行日志 |
| `docker exec pgvector-db-new psql -U postgres -d postgres -c "SELECT * FROM sutra_feedback_metrics;"` | 查看反馈指标 |
| `docker exec pgvector-db-new psql -U postgres -d postgres -c "SELECT id, task_success, substring(query,1,30) AS q, domain FROM sutra_execution_logs ORDER BY id;"` | 查看执行日志摘要 |
| `grep "FeedbackAnalyzer" logs/subhuti.log* \| tail -10` | 查看反馈分析器日志 |
| `grep "共现挖掘" logs/subhuti.log* \| tail -5` | 查看共现挖掘结果 |

### 10.6 测试用例

| 测试文件 | 说明 |
|----------|------|
| `crates/subhuti-infra/tests/sutra_library_live.rs` | 端到端集成测试（13 个用例） |
| `crates/subhuti-infra/src/sutra_library/recall/scoring.rs` | 归一化测试 |
| `crates/subhuti-infra/src/sutra_library/recall/query.rs` | 查询分析测试 |
| `crates/subhuti-infra/src/sutra_library/recall/entity_extractor.rs` | 实体提取测试 |

---

## 附录：数据流总结

### 新增记忆 → 图谱自动学习

```
用户输入 → BlenderExpert.run()
  → sutra.add_session()         [临时记忆]
  → sutra.write()               [持久记忆 + 实体图谱注册 + Manual 边]
    → parser.split_semantic_chunks()
    → parser.extract_entity_relations() → EntityGraph.add_manual_edge()
    → entity_graph.register_chunk()     → 实体索引
  → 异步 PG 落库
```

### 查询 → 召回 → LLM 回答 → 反馈闭环

```
用户问题 → BlenderExpert.run()
  → sutra.library_retrieve()    [五阶段召回流水线]
    → QueryAnalyzer.analyze()   [查询理解]
    → BaseSearch.search()       [BM25 种子]
    → SpaceDepthStrategy        [空间邻居]
    → GraphPassageStrategy      [图谱 BFS]
    → normalize + sort          [归一化排序]
  → 注入 system prompt          [历史知识]
  → LLM.chat()                  [生成回答]
  → sutra.record_execution()    [反馈闭环入口]
    → FeedbackAnalyzer          [滑动窗口记录]
      → 分析命中率
      → 实体共现挖掘
      → 更新 Learned 边
      → PG 持久化
```

### 完整记忆自进化闭环

```
用户Query → 藏经阁召回一批记忆切片 → LLM/Agent执行任务
    ↓
【反馈采集层】两条反馈来源
    ① LLM自省打分：召回出来的切片有没有真正用上
    ② 执行日志埋点：实际哪些记忆切片被Agent引用、输出
    ↓
【数据分析模块】统计命中率、共现频率、候选切片共现
    ↓
自动生成/更新图谱 Learned动态边（实体‑实体熟练度）
    ↓
持久化回PG，同步刷新petgraph内存图谱
```
---

## 11. 接入外部 MCP 客户端（WorkBuddy）

### 11.0 现状一句话

**服务端已完全就绪**（6 个工具 + 藏经阁 4 个 action 实测可用）；
**只差客户端侧一步手动"信任"**——配置写进 `mcp.json` 不会自动生效。

### 11.1 服务端自检（不需要 WorkBuddy）

标准 stdio 握手，确认工具清单与藏经阁可用：

```bash
cd /path/to/subhuti-app
SUBHUTI_DATA_DIR=/Users/hezenghui/sqlite python3 - << 'PY'
import json, os, subprocess, time
p = subprocess.Popen(["target/debug/subhuti", "mcp"], stdin=subprocess.PIPE,
                     stdout=subprocess.PIPE, text=True, bufsize=1,
                     env={**os.environ, "SUBHUTI_DATA_DIR": "/Users/hezenghui/sqlite"})
def send(m): p.stdin.write(json.dumps(m) + "\n"); p.stdin.flush()
send({"jsonrpc":"2.0","id":1,"method":"initialize","params":{
    "protocolVersion":"2024-11-05","capabilities":{},
    "clientInfo":{"name":"probe","version":"1"}}})
print(json.loads(p.stdout.readline())["result"]["instructions"][:80], "...")
send({"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}})
while True:
    m = json.loads(p.stdout.readline())
    if m.get("id") == 2:
        print([t["name"] for t in m["result"]["tools"]]); break
p.terminate()
PY
```

期望：`initialize` 返回 `instructions`，`tools/list` 含 `subhuti_memory`。

### 11.2 客户端配置（`~/.workbuddy/mcp.json`）

```json
{
  "mcpServers": {
    "subhuti": {
      "command": "/Users/hezenghui/RustroverProjects/subhuti-app/target/debug/subhuti",
      "args": ["mcp"],
      "env": { "SUBHUTI_DATA_DIR": "/Users/hezenghui/sqlite" },
      "disabled": false
    }
  }
}
```

### 11.3 激活：必须点"信任"（唯一的手动步骤）

配置写入后 **不会自动生效**。需要在 WorkBuddy 的
**连接器管理页右上角 → 自定义连接器** 里对 `subhuti` 点 **信任**，才会被拉起。

**怎么判断有没有生效**（两个客观判据）：

| 判据 | 未生效 | 已生效 |
|------|--------|--------|
| 运行时目录 | 无 `~/.workbuddy/logs/mcp-runtime/custom-mcp_subhuti-*` | 存在该目录 |
| 会话工具列表 | 看不到 `mcp__subhuti__*` | 可用 6 个 `subhuti_*` 工具 |

> 注：`mcp-runtime/custom-mcp_blender-mcp-036058ed` 是已生效的对照组——
> 有目录 = WorkBuddy 真的拉起过这个 server。

### 11.4 让 WorkBuddy "自动"用记忆

接上之后有两条使用路径，**语义不同**：

| 路径 | 触发方式 | 行为 |
|------|----------|------|
| `subhuti_memory` | Agent 显式调用 | recall / write / stats / collections 单点操作。**不自动**，靠 Agent 判断时机 |
| `subhuti_chat` | Agent 显式调用 | 走完整编排层：专家路由 + 藏经阁 RAG 召回 + **会话结束自动沉淀** |

为了减少"要用户提醒才想起来用"，`initialize` 已下发 **`instructions`** 字段
（`InitializeResult.instructions`），把调用时机写清楚：回答前先 `recall`、
得知长期事实后 `write`。客户端会把它作为系统级说明注入上下文。

> 这是 MCP 协议里引导客户端主动使用某个 server 的**唯一标准机制**。
> 没有它，藏经阁就退化成"手动提醒才用"的工具。

### 11.5 三个必踩的坑

| 坑 | 说明 | 规避 |
|---|---|---|
| **debug 二进制** | `mcp.json` 指向 `target/debug/subhuti`。改了代码不 `cargo build`，MCP 跑的还是旧逻辑，表现为"改了没生效" | 改完代码先 `cargo build --bin subhuti`，再重连 MCP |
| **数据目录不一致** | `SUBHUTI_DATA_DIR` 必须与项目运行时一致；否则 MCP 读到的是**另一套空库**，表现为"记忆一条都没有" | 固定用 `/Users/hezenghui/sqlite` |
| **cwd 不在项目内 → LLM 全挂** | `ZHIPU_API_KEY` **只从环境变量取**（`Subhuti.toml` 里刻意不写 key），而它的唯一来源是项目根的 `.env`；`.env` 靠 `dotenvy::dotenv()` 从 **cwd 向上**查找，**没有任何兜底**。WorkBuddy 拉起子进程时不设 cwd → 读不到 `.env` → `subhuti_chat` 直接 `HTTP 401`，连带"chat 结束后自动沉淀"一起失效 | 在 `mcp.json` 的 `env` 里显式补 `"ZHIPU_API_KEY"`（推荐）；用 `scripts/debug/mcp_env_probe.py` 一条命令自检 |

**为什么前两个坑好排查、第三个坑阴**：`config/Subhuti.toml` 和 `.env` 的定位机制**不一样**——

| 资源 | 定位方式 | 项目外 cwd |
|---|---|---|
| `config/Subhuti.toml` | 先找 `./config/Subhuti.toml`，**找不到就退到 `env!("CARGO_MANIFEST_DIR")/config/Subhuti.toml`**（编译期把源码路径烧进 debug 二进制） | ✅ 仍能找到 |
| `.env` | `dotenvy::dotenv()`，只从 cwd 向上，**无兜底** | ❌ 找不到 |

于是出现"配置读到了、key 读不到"的割裂现象：`stats`/`recall`/`experts` 一切正常，
**只有需要 LLM 的路径静默失败**。实测同一份二进制、同一个 session：

| cwd | `subhuti_chat` | 耗时 |
|---|---|---|
| `/tmp`（= WorkBuddy 情形） | ❌ `Zhipu API HTTP 401: 令牌已过期或验证不正确` | 0.4s |
| 项目根 | ✅ 正常回答，`tokens=1024(2次LLM调用)`，并触发 `沉淀 N 条记忆` | 18.6s |

### 11.5.1 接上之后的能力边界

把 LLM key 补上之前，先认清哪些能力是通、哪些不通——**别把"藏经阁读取正常"误判成"整个 server 都通了"**：

| 能力 | 依赖 | 项目外 cwd |
|------|------|-----------|
| `subhuti_memory`（recall / write / stats / collections） | 只读/写库 | ✅ 可用 |
| `subhuti_list_experts` / `subhuti_match_expert` | 专家注册表 | ✅ 可用 |
| `subhuti_chat` / `subhuti_skill_run` | LLM key | ❌ 401 |
| chat 后的**自动沉淀**（藏经阁自进化闭环的核心） | LLM key | ❌ 连带失效 |

也就是说：**缺 key 时藏经阁退化成"只能读、得 Agent 手写"**。日常"回答前 recall、得知事实后 write"
仍然成立（这两条都不走 LLM），但"聊完自动记住"这条最有价值的链路是断的。

### 11.6 授权机制与排查线索

**"信任"不是藏经阁的特殊待遇**——它是 WorkBuddy 对**所有本地自定义 MCP server** 的统一安全机制。
任何自定义 server（blender-mcp 亦然）首次使用前都要点一次信任，之后长期有效。

**实证**（2026-09-14 核查）：`~/.workbuddy/audit-log/*.jsonl` 中 `category=mcp` 的记录，
从 2026-08-29 至今**只有 1 条**：

```
2026-08-30 23:11:14  mcp__blender-mcp__blender_generate_model  allowed
```

而 `~/.workbuddy/mcp-approvals.json` 中 blender-mcp 有**两条**审批时间戳
（`08-30 03:32:29`、`08-30 22:47:28`）——**先信任（22:47）才能调用（23:11）**。
两条说明审批按「配置指纹 :: server 名」记录，**配置或作用域变化会再要一次**。

**为什么会产生"以前用 MCP 调过却不用点信任"的错觉**：两种情况都会造成这种印象 ——

1. 以往在项目里是用 **Bash 手工起 `subhuti mcp` stdio 进程**（自己 spawn 子进程）做验证，
   **绕过了 WorkBuddy 的托管 → 不需要授权**；
2. 真正走托管的那次（8-30 blender-mcp）确实点过信任，只是时间久了容易忘记。

**排查三线索**：

| 线索 | 位置 | 含义 |
|------|------|------|
| 审批记录 | `~/.workbuddy/mcp-approvals.json` | key = `指纹::server名`；存在 = 已点过信任 |
| 运行时目录 | `~/.workbuddy/logs/mcp-runtime/custom-mcp_<名>-*` | 存在 = 该 server 真被拉起过 |
| 调用审计 | `~/.workbuddy/audit-log/*.jsonl`（`category == "mcp"`） | 真实的 MCP 工具调用流水 |

> `mcp-runtime/` 下的 `custom-mcp_blender-mcp-036058ed/`（8-30 建）是"已生效"的对照组。

**本机当前状态（2026-09-14 01:37）**：`subhuti` 已完成审批（`mcp-approvals.json` 出现
`5c2f418c…::subhuti`），但运行时目录尚未创建 → **需开新会话**才会被拉起并挂上
`mcp__subhuti__*` 六个工具（当前会话启动于 01:14，早于审批时刻）。

### 11.7 把藏经阁当「纯记忆引擎」用（只保存 + 搜索）

如果只想让外部客户端用藏经阁的**记忆能力**（不跑专家编排），只看
`subhuti_memory` 的 `write` / `recall` / `stats` 三个 action 即可。
这条链路**不依赖 LLM，也不依赖 `ZHIPU_API_KEY`** —— 与 §11.5 的坑完全无关。

**验证方式**：`scripts/debug/mcp_memory_engine_probe.py`（8 阶段 42 项断言，
用独立测试库 `/tmp/subhuti-memtest`，不碰真实库；调用姿势复刻客户端：cwd 在项目外）。

| 阶段 | 测什么 | 结果 |
|------|--------|------|
| 冷启动 | 空库 stats=0；异步种子可见性 | ✅ |
| 写入 | 6 条事实全部落库；重复写幂等；意图句拒写；空白拒写 | ✅ |
| 字面召回 | 6 条关键词查询，**正确答案全部排第 1** | ✅ |
| 同义召回 | 6 条改写查询（「报错怎么处理」→「错误处理统一用 anyhow」），**全部排第 1** | ✅ |
| top_k / 拒答 | `top_k=1` 只返回 1 条；无关查询返回"未找到匹配的记忆" | ✅ |
| **跨进程持久化** | 关掉子进程重开，节点数不变、排序不变 | ✅ |
| 规模 | 累积到 23 条后，早期 6 条仍全部排第 1 | ✅ |
| 真实库 | 既有 8 条记忆可直接搜到 | ✅ |

**结论**：作为"保存 + 搜索"的记忆引擎**可用**（42/42）。

#### 用之前必须知道的三件事

**① 冷启动会自动灌 4 条 Rust 静态知识**（`SUBHUTI_DISABLE_SEED=1` 可关）

`composition_root.rs` 在装配后 `tokio::spawn` + **固定 sleep 800ms** 才灌种子
（幂等，已存在则不重复）。对"纯记忆引擎"用途这是污染：

- 空库启动后约 1~2 秒，`stats` 从 0 变 4；你没写任何东西却多出 4 条；
- 这些节点会以低分混进召回结果，白占 `top_k` 名额；
- 注入是异步的，**期间 stats 与检索结果会抖动** —— 第一轮探针里 4 项"失败"
  全是这个引起的**假失败**（不等稳就断言）。

设 `SUBHUTI_DISABLE_SEED=1` 即关闭，默认行为不变。

**② `top_k` 才是"最终条数"**（本轮修的 bug）

此前 `base_top_k=top_k` 而 `graph_max_global_result=top_k*2`，两路各自取数、
合并后**从不截断** → `recall(top_k=1)` 返回 2 条，多出的那条是
0.075~0.3 分的共现邻居噪声。已修，并加回归测试 `top_k_caps_final_result_count`。

**③ 同义召回靠分词重叠，不是向量语义**

检索后端是 Tantivy（jieba 分词 + OR 布尔）。上表"同义召回"能过，
是因为改写句与原文共享「渲染/目录」「日志/库」这类词项。
**零字面重叠**的纯语义查询（如「怎么记录运行信息」→「日志库选 tracing」）
大概率搜不到。把记忆正文写得含关键词，召回率会显著更好。

#### 建议的接入配置

```json
{
  "mcpServers": {
    "subhuti": {
      "command": "/Users/hezenghui/RustroverProjects/subhuti-app/target/debug/subhuti",
      "args": ["mcp"],
      "env": {
        "SUBHUTI_DATA_DIR": "/Users/hezenghui/sqlite",
        "SUBHUTI_DISABLE_SEED": "1"
      },
      "disabled": false
    }
  }
}
```

> `ZHIPU_API_KEY` **对记忆链路非必需**，只有 `subhuti_chat` / `subhuti_skill_run`
> 才要（见 §11.5）。

---

## 12. 召回质量治理（2026-09-14）

### 12.0 一句话

此前藏经阁"能召回但排序不可靠"：答案不漏，却常被**图谱扩召回蹭上来的无关记忆**
挤到第 2 位（实测 6 条查询中 3 条如此）。

### 12.1 四个病症与根因

| # | 现象 | 根因 | 修复位置 |
|---|------|------|----------|
| 1 | 无关记忆顶掉精确命中 | 四条召回通路**各自 rank 归一化**，每条通路第一名恒为 `1.0`，再直接相加 → 弱通路（图谱）的头名与强通路（全文检索）的头名**等权** | `recall/scoring.rs`：引入通路权重 BASE=1.0 / SPACE=0.5 / GRAPH1=0.3 / GRAPH2=0.15，各通路独立算分后**取 max 合并**（顺带修掉旧实现"三条扩展通路共用 `bonus_score` 互相覆盖"的问题） |
| 2 | 碎片成为跨主题枢纽 | 实体是 2-4 字字符滑窗 ngram，`link_cooccurrence` 取前 8 个两两建边 → 「探针」「块职责」这类碎片把语义无关的记忆连成邻居 | `graph.rs::select_cooccur_entities`：**≥3 字**且**不被同批更长实体包含**才可建边；节点自身 UUID 也不参与共现 |
| 3 | 垃圾记忆占满结果 | LLM 把**提问意图**当事实沉淀（「关注 X 在项目中的角色」），一次 7 条结果里占 4 条 | `MemoryConsolidator::is_intent_sentence`（**六段**判据，见 12.6）拦在落库前；MCP 手动 `write` 复用同一判据 |
| 4 | 0 分 / 空内容条目入结果 | rank 归一化后每条通路末位恒为 0，却照样输出；无标题节点渲染成 `(无标题)` | `engine.rs::library_retrieve` 输出侧过滤 `total<=1e-6` 与三字段全空；无标题时用正文开头代标题 |

### 12.2 关键设计变更：边的真相

- **旧**：图谱边从 `graph_edges` 表回灌（`hashmap_to_graph` + (min,max) 去重）。
- **新**：边的唯一真相 = **`graph_entity_chunks`（切片 → 实体）+ 当前建边规则**，
  启动时按规则重建（`load_persisted_data` / `hydrate_graph`）。

好处：建边规则一收紧，图谱**自动自愈**，不需要写迁移脚本去清历史边。
连带删除死代码 `hashmap_to_graph`。

### 12.3 顺带修掉的全文索引不同步

手工删除节点后，Tantivy 磁盘索引里仍留着已删节点的文档。这些"幽灵文档"会被检索
命中、却 `read_node` 读不到，**白占 `top_k` 名额** —— 实测 `top_k=1` 时直接返回
"未找到匹配的记忆"（而库里明明有数据）。

> `hydrate()` 现在比对 `num_docs()` 与库中节点数，不一致就 `clear()` 后整体重建。

### 12.4 自查脚本

| 脚本 | 回答什么问题 |
|------|--------------|
| `scripts/debug/mcp_memory_probe.py` | 能力矩阵：工具 / action / write / recall / stats 是否可用，含**意图句拒写**（18 项） |
| `scripts/debug/recall_quality_probe.py` | **排序质量**：正确答案是否在**第 1 条**、对照查询是否拒答、有无脏项（7 用例） |
| `scripts/debug/cleanup_probe_data.py` | 清理探针残留 + 提问意图垃圾 + 清空待重建的边（默认**预演**，`--apply` 才动手，且先备份）；`--self-test` 校验判据与 Rust 侧同源 |
| `scripts/debug/mcp_env_probe.py` | **接入自检**：按 WorkBuddy 的真实调用姿势（项目外 cwd）验证各能力是否可达；`--no-llm` 跳过付费调用（见 §11.5） |

```bash
cargo build --bin subhuti
SUBHUTI_BIN=target/debug/subhuti SUBHUTI_DATA_DIR=~/sqlite \
  python3 scripts/debug/recall_quality_probe.py
```

### 12.5 验收结果

| 套件 | 结果 | 覆盖 |
|------|------|------|
| `cargo test --workspace` | **183 passed, 0 failed** | 新增回归：通路加权、碎片过滤、意图拦截、边由索引派生 |
| `mcp_eval.py` | **29/29** | 工具清单 / 生命周期 / 报错 / 并发 |
| `mcp_eval_deep.py` | **15/15** | 深水区 / system_prompt / 输入边界 / 并发隔离 |
| `mcp_memory_probe.py` | **18/18** | 藏经阁能力矩阵（含 3 条意图句拒写） |
| `recall_quality_probe.py` | **7/7** | **排序质量**（修复前 6 条查询里 3 条答案是第 2 位） |

### 12.6 第二轮：入口收敛与测试残留（同日稍晚）

第一轮收工后重跑全套，库里又攒出两类**新**噪声——说明"拦一个入口"不够，
判据必须覆盖**全部落库入口**、清理必须覆盖**全部测试写死会话**。

| # | 新噪声 | 样本 | 处置 |
|---|--------|------|------|
| 5 | 第三人称复述 | 「用户关注 Rust 模块职责」「用户提及项目代码和模块」 | `is_intent_sentence` 第 ③ 段：`用户/我` 开头 + 泛意图动词 |
| 6 | 话题标题式 | 「Blender 修改器介绍」「布尔运算在Blender中的应用」 | 第 ④ 段：≤25 字且以 介绍/描述/说明/应用/作用/概述 结尾 |
| 7 | **通用知识句** | 「Rust 模块支持抽象和复用功能」「Rust 模块用于组织代码和封装」 | 第 ⑤ 段：**无用户锚点**（用户/我/本项目/项目/偏好/默认/固定/配置/约定…）+ 能力谓语（支持/用于/包括/分为/属于/…之分） |
| 8 | 动作片段 | 「了解几何修改器」「探索修改器类型」 | 第 ① 段前缀扩至 了解/探索/学习/研究/查看 |
| 9 | **MCP 手动 write 通道** | 与自动沉淀同类的意图句可从 `subhuti_memory(action="write")` 漏进库 | `is_worth_remembering` 提为 `pub(crate)`，MCP write 分支复用，命中即 `Err` |
| 10 | 测试脚本写死的 session 残留 | `eval-sticky-1` / `edge-emoji` / `iso-N` / `probe-<ts>` | `cleanup_probe_data.py` 的 `PROBE_PATH_HINTS` 增补（**不是猜测**，是从脚本里核对出的字面 id） |

为什么第 ⑤ 段必须存在：这类句子与用户提问共享「Rust 模块」等 n-gram，
`grounded_in`（字面 2-gram 重叠 ≥ 20%）**拦不住**——它确实是"用户提到过的话题"，
只是不是"关于用户的事实"。落库前是唯一能拦的地方。

> ⚠️ 注意：⑤ 段刻意留了**锚点豁免**。「本项目支持多租户」「用户项目支持插件扩展」
> 这类带锚点的能力描述是真事实，不能被误杀；两条都已进回归用例锁死。

**判据同源**：`cleanup_probe_data.py` 与 Rust 侧共用一张判据表，
`--self-test` 固化 22 条垃圾 + 13 条真实事实，两端一漂移就报警。

### 12.7 测试侧的两个必修项

治理闭环最后一圈暴露的**测试自身**问题（不改产品逻辑，但不修就无法自证）：

| # | 问题 | 症状 | 修法 |
|---|------|------|------|
| 11 | 测试脚本不传 `session_id` | 服务端生成随机 UUID 会话，沉淀残留路径**每次跑都变** → 清理脚本永远抓不到（实测漏过 4 条「Rust 语言」类） | 7 处 `subhuti_chat` 调用补显式 `session_id`（`eval-*` / `edge-*` / `iso-*` 前缀）；`PROBE_PATH_HINTS` 由字面 id 泛化为 `/session/eval-`、`/session/edge-` |
| 12 | `data_dir::tests` 不做环境隔离 | 一旦 `export SUBHUTI_DATA_DIR`（跑探针的常规操作），`default_dir_is_under_home` 立刻红 | 改为按**文档化优先级**断言：设了就必须原样胜出，没设才断言默认目录 |

> ⚠️ 连带约束：`mcp_eval.py` 里两次 `subhuti_chat` **必须用不同 session**，
> 否则第 2 次（领域外提问）会靠会话粘性继承 rust 领域，`isError=true` 断言假失败。

**验证方式**：`cargo test --workspace` 在**带 / 不带 `SUBHUTI_DATA_DIR`** 两种环境下各跑一遍，
均应 183 passed / 0 failed。

**第二轮验收**：`cargo test --workspace` 183 passed / 0 failed；
`recall_quality_probe` 7/7；`mcp_memory_probe` 18/18；`mcp_eval` 29/29；
`mcp_eval_deep` 15/15。清理后库内 **8 条**（4 条知识种子 + 3 条真实配置 + 1 条偏好），
`graph_entity_chunks` 归零——被删节点独占该表，存留节点本就未登记索引，
**无数据损失**；新写入记忆时图谱照常重建（实测 `graph_edges 0→28`）。

### 12.8 第四轮：锚点表里的裸「项目」（2026-09-14 02:40）

这一轮**不是跑测试发现的，而是做接入验证时撞出来的**——按 WorkBuddy 的真实调用姿势
跑了一次 chat（项目根 cwd 那一组），它自动沉淀的两条记忆里有一条是
「项目涉及 Rust 编程」，明文落库。

| # | 问题 | 症状 | 修法 |
|---|------|------|------|
| 13 | ⑤ 段锚点表含**裸「项目」** | 「项目涉及 Rust 编程」被判定为"有用户锚点" → 豁免 → 落库 | `ANCHORS` 去掉裸「项目」（只留「本项目」）；`GENERIC_PREDICATES` 补「涉及」 |

**为什么会漏**：⑤ 段的设计前提是"**无**用户锚点 + 能力谓语 = 通用知识句"。
但锚点表里放了裸「项目」之后，「项目涉及 X」这类**泛话题句**恰好含「项目」，
被当成"有锚点"直接豁免。区分真伪其实只需要更精确的指代：
真事实是「**本项目**支持多租户」，泛话题是「**项目**涉及 Rust 编程」。

**删锚点会不会误伤**：真事实「项目上下文」不含能力谓语，删掉裸「项目」后
依然能通过（已进回归用例 `real_facts_survive_intent_filter` 锁死）。

**判据表两端同步**：`cleanup_probe_data.py` 的 `ANCHORS` / `GENERIC_PREDICATES`
一并修改，`--self-test` 基准更新为 **22 条垃圾 + 13 条真实事实**。

**本轮顺带产出**：`scripts/debug/mcp_env_probe.py`（见 §11.5）——
一条命令回答"在 WorkBuddy 的实际调用姿势下，哪些能力是通的"，
把「以为接好了」变成可重复的断言。

**第四轮验收**：`cargo test --workspace` 183 passed / 0 failed（带 / 不带
`SUBHUTI_DATA_DIR` 两种环境）；`memory_consolidation` 7/7；库内 **8 条**、0 待清。

### 12.9 第六轮：当「纯记忆引擎」验（2026-09-14 02:50）

**起因**：用户要求验证"藏经阁能否当 WorkBuddy 的记忆引擎（只保存 + 搜索）"，
`subhuti_chat` 先不管。新增 `scripts/debug/mcp_memory_engine_probe.py`
（独立测试库 `/tmp/subhuti-memtest`、cwd 在项目外、8 阶段 42 项）——见 §11.7。

这一轮**不是跑旧套件发现的**，而是换了个使用姿势（只读写记忆、不用 LLM）
之后暴露出来的：

| # | 问题 | 症状 | 修法 |
|---|------|------|------|
| 14 | **`top_k` 完全不生效** | `recall(top_k=1)` 返回 **2 条**（Base 1 + GraphFirstPass 1），多出的那条是 0.075~0.3 分的共现邻居噪声 | 根因：`base_top_k=top_k` 而 `graph_max_global_result=top_k*2`，两路各自取数、**合并后从不截断**。修：输出循环加 `if shown >= top_k { break }`；缓存口径同步改为「实际返回的 chunk_ids」（原先缓存全部候选，"召回 N 条"统计与调用方所见不一致）。回归测试 `top_k_caps_final_result_count` |
| 15 | **④ 段话题尾误杀真事实** | 「提交信息用中文描述」以「描述」收尾且 ≤25 字 → 被当话题标题**拒写** | 加豁免词（用/是/为/必须），但**只检查尾巴之前的部分** |

**#15 的坑中坑**（值得单独记）：豁免词第一版写的是"对整句 `contains("用")`"，
结果「布尔运算在Blender中的**应用**」「细分算法在Blender中作**用**」自身就含「用」，
被一并放过 —— **两端的 `--self-test` 立刻报了出来**（22 条垃圾里漏判 2 条）。
这正是"判据必须两端同源"的价值：Rust 侧改完 Python 侧自检就能拦下不一致。

**同时新增一个开关**（非 bug，属配置能力）：`SUBHUTI_DISABLE_SEED=1`
关闭冷启动种子灌入（**默认行为不变**）。理由见 §11.7 —— 把藏经阁只当记忆引擎的
客户端不该凭空多出 4 条 Rust 静态知识，而且异步注入期间 stats/检索会抖动。

**第六轮验收**：

| 套件 | 结果 |
|------|------|
| `cargo test --workspace` | **184 passed / 0 failed**（带 / 不带 `SUBHUTI_DATA_DIR` 各一遍） |
| `mcp_memory_engine_probe.py` | **42/42**（默认模式 + `--no-seed` 模式各一遍） |
| `memory_consolidation` | **7/7** |
| `cleanup_probe_data --self-test` | 22 条垃圾 + 14 条真实事实 全对 |
